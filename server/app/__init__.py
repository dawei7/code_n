"""cOde(n) FastAPI app package.

The :mod:`server.app` package holds the FastAPI application factory
(``main.py``), the engine runner (``engine_runner.py``), the
trace codec (``trace_codec.py``), the progress store wrapper
(``progress_store.py``), the exception handlers (``error_handlers.py``),
and the route modules in ``routes/``.
"""
