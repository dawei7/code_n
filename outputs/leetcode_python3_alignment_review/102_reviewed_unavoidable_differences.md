# Reviewed unavoidable Optimal-solution differences

Generated from the live hash-bound `alignment_review.json` files after the full 1–4005 alignment audit.

## Summary

- Reviewed packages: **102**
- Recorded differences: **104**
- Current audit result: **3,903 structurally aligned, 102 reviewed unavoidable differences, 0 review required, 4,005 total**
- Classification counts: **88** SQL dialect, **20** app execution adapter, **15** platform signature, and **11** judge environment. A package may carry more than one classification.
- Every review is bound to the current app source, native source, variant manifest, and approach hashes. The alignment audit rejects stale hashes.

## Detailed reasons

| ID | Problem | Language | Classification | Difference | Native source form | App-local form | Why unavoidable | Review evidence |
|---:|---|---|---|---|---|---|---|---|
| 177 | Nth Highest Salary | `mysql` | `sql_dialect`<br>`app_execution_adapter` | function argument environment | MySQL receives N as a stored-function argument, decrements it, and uses it as LIMIT OFFSET. | SQLite reads N from the one-row Request fixture and computes N - 1 directly in LIMIT OFFSET. | Both select the same distinct descending salary at zero-based offset N - 1 and return null when that row does not exist. | [alignment_review.json](../../dsa/leetcode/0177_nth-highest-salary/variants/optimal/alignment_review.json) |
| 196 | Delete Duplicate Emails | `mysql` | `sql_dialect`<br>`app_execution_adapter` | 1. multi-table delete syntax<br>2. mutation result observation | 1. MySQL deletes each duplicate joined to a same-email keeper with a smaller id.<br>2. LeetCode observes the mutated Person table after the DELETE statement. | 1. SQLite deletes each Person row for which an EXISTS subquery finds the same smaller-id keeper.<br>2. The local script selects id and email after deletion so the isolated runner can compare the resulting table; case validation ignores row order. | 1. Both use the identical duplicate-versus-keeper predicate; the correlated form is the SQLite-compatible expression of the MySQL self-join delete.<br>2. The additional SELECT does not participate in deletion and exists only at the app execution boundary. | [alignment_review.json](../../dsa/leetcode/0196_delete-duplicate-emails/variants/optimal/alignment_review.json) |
| 197 | Rising Temperature | `mysql` | `sql_dialect` | one-day date comparison | MySQL uses DATEDIFF(current.recordDate, previous.recordDate) = 1. | SQLite compares julianday values with a difference of one. | Both self-join exactly consecutive calendar dates and apply the identical temperature predicate, projection, aliases, and complexity. | [alignment_review.json](../../dsa/leetcode/0197_rising-temperature/variants/optimal/alignment_review.json) |
| 277 | Find the Celebrity | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | judge-supplied relationship API | LeetCode calls findCelebrity(n) and supplies the global knows(a, b) predicate. | solve(n, knows_matrix) receives the authored JSON relationship fixture and defines knows(a, b) as its direct matrix lookup. | The offline runner must receive the hidden relationship as explicit deterministic case data. After this one-line environment adapter, candidate elimination and verification use the same predicate calls, control flow, and query complexity. | [alignment_review.json](../../dsa/leetcode/0277_find-the-celebrity/variants/optimal/alignment_review.json) |
| 278 | First Bad Version | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | judge-supplied monotone predicate | LeetCode calls firstBadVersion(n) and supplies the global isBadVersion(version) predicate. | solve(n, bad) receives the authored JSON boundary and defines isBadVersion(version) as version >= bad. | The offline runner must expose the hidden boundary as explicit deterministic case data. After this one-line environment adapter, both artifacts execute the same boundary-preserving binary search and make the same number of predicate calls. | [alignment_review.json](../../dsa/leetcode/0278_first-bad-version/variants/optimal/alignment_review.json) |
| 374 | Guess Number Higher or Lower | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | judge-supplied comparison API | LeetCode calls guessNumber(n) and supplies the global guess(num) oracle for its hidden value. | solve(n, pick) receives the authored hidden value explicitly and defines guess(num) with the same three-way result. | The offline runner must receive the otherwise hidden value as deterministic case data. After this environment adapter, both forms execute the same binary-search statements, oracle calls, control flow, and logarithmic query complexity. | [alignment_review.json](../../dsa/leetcode/0374_guess-number-higher-or-lower/variants/optimal/alignment_review.json) |
| 470 | Implement Rand10() Using Rand7() | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | judge-supplied random API and repeated app execution | LeetCode calls rand10() once per request and supplies the hidden global rand7() random API. | solve(rand7_values, draws) defines rand7() over an authored deterministic cyclic stream, defines the same single-result rand10() rejection sampler, and calls it for the requested number of outputs. | The offline runner must expose hidden randomness as reproducible case data and return a finite trace for validation. Each app rand10() call executes the same two-oracle-call cell construction, rejection condition, output mapping, and expected constant work as the native method. | [alignment_review.json](../../dsa/leetcode/0470_implement-rand10-using-rand7/variants/optimal/alignment_review.json) |
| 478 | Generate Random Point in a Circle | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | hidden random source and repeated object calls | LeetCode constructs Solution(radius, x_center, y_center); each randPoint() call obtains two hidden random() values. | solve receives the same constructor values plus an authored random_values stream and draws count, defines a local random() source, and invokes the same randPoint calculation for each requested result. | An offline deterministic judge must expose random inputs and a finite call count. For each point, both forms apply the identical square-root radial transform, angle transform, center translation, and constant-time arithmetic. | [alignment_review.json](../../dsa/leetcode/0478_generate-random-point-in-a-circle/variants/optimal/alignment_review.json) |
| 497 | Random Point in Non-overlapping Rectangles | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | hidden random source and repeated object calls | LeetCode constructs Solution(rects); each pick() call obtains a hidden randrange(total) ticket. | solve receives rects plus an authored random_values stream and draws count, derives each deterministic randrange(total) ticket, and invokes the same pick calculation for every requested result. | An offline deterministic judge must expose random inputs and a finite call count. Prefix construction, weighted ticket lookup, rectangle offset decoding, and per-pick logarithmic complexity are identical after the random-source adapter. | [alignment_review.json](../../dsa/leetcode/0497_random-point-in-non-overlapping-rectangles/variants/optimal/alignment_review.json) |
| 519 | Random Flip Matrix | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | hidden random source and persistent operation sequence | LeetCode constructs Solution(m, n), supplies randrange(), and calls flip() or reset() on the persistent object. | solve(m, n, random_values, operations) derives deterministic randrange tickets and dispatches the authored flip/reset trace over the same persistent state. | The offline judge must expose randomness and method calls as reproducible data. The virtual-array state, sparse Fisher-Yates remapping, flip return, reset behavior, and expected constant-time operations are otherwise identical. | [alignment_review.json](../../dsa/leetcode/0519_random-flip-matrix/variants/optimal/alignment_review.json) |
| 528 | Random Pick with Weight | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | hidden random source and repeated object calls | LeetCode constructs Solution(w); each pickIndex() obtains a hidden random() value. | solve(w, random_values) defines random() over the authored values and calls the same pickIndex calculation once per value. | The offline judge must expose randomness and a finite call count. Prefix sums, total weight, scaled target, bisect_right selection, and per-pick logarithmic complexity are identical after the random-source adapter. | [alignment_review.json](../../dsa/leetcode/0528_random-pick-with-weight/variants/optimal/alignment_review.json) |
| 550 | Game Play Analysis IV | `mysql` | `sql_dialect`<br>`app_execution_adapter` | date arithmetic and division semantics | The verified MySQL query uses DATE_ADD(first_login, INTERVAL 1 DAY) and MySQL division. | The offline SQLite query uses date(first_login, '+1 day') and multiplies the numerator by 1.0 to prevent integer division. | These are the respective dialect-native spellings of the same next-calendar-day join and fractional aggregation. The CTE, grouping, join keys, numerator, denominator, rounding, result schema, and complexity are unchanged. | [alignment_review.json](../../dsa/leetcode/0550_game-play-analysis-iv/variants/optimal/alignment_review.json) |
| 615 | Average Salary: Departments VS Company | `mysql` | `sql_dialect`<br>`app_execution_adapter` | month formatting function | The verified MySQL query derives pay_month with DATE_FORMAT(pay_date, '%Y-%m'). | The offline SQLite query derives pay_month with strftime('%Y-%m', pay_date). | These are dialect-equivalent month keys. The joins, window partition, aggregates, grouping, comparison, ordering, result schema, and complexity are otherwise identical. | [alignment_review.json](../../dsa/leetcode/0615_average-salary-departments-vs-company/variants/optimal/alignment_review.json) |
| 627 | Swap Sex of Employees | `mysql` | `app_execution_adapter`<br>`platform_signature` | mutation result exposure | LeetCode observes the Salary table after the single UPDATE statement and therefore needs no result query. | The offline runner executes the identical UPDATE and then selects Salary in id order so authored cases can observe the mutated table. | The appended read-only SELECT is required by the function-style offline result channel. It does not alter the update expression, affected rows, stored values, or linear mutation complexity. | [alignment_review.json](../../dsa/leetcode/0627_swap-sex-of-employees/variants/optimal/alignment_review.json) |
| 710 | Random Pick with Blacklist | `python3` | `judge_environment`<br>`app_execution_adapter`<br>`platform_signature` | hidden random source and repeated object calls | LeetCode constructs Solution(n, blacklist); each pick() obtains a hidden randrange(bound) draw. | solve(n, blacklist, draws) defines randrange(bound) over the authored integer tickets and calls the same pick calculation once per ticket. | The offline judge must expose randomness and a finite call count. Blacklist preprocessing, low-to-high remapping, raw ticket lookup, uniform-domain bijection, and expected constant-time picks are identical after the deterministic random-source adapter. | [alignment_review.json](../../dsa/leetcode/0710_random-pick-with-blacklist/variants/optimal/alignment_review.json) |
| 1097 | Game Play Analysis V | `mysql` | `sql_dialect` | one-day date arithmetic | MySQL computes the day after installation with DATE_ADD(i.install_dt, INTERVAL 1 DAY). | SQLite computes the same date with date(i.install_dt, '+1 day'). | The installed SQL runner uses SQLite, which does not implement MySQL's DATE_ADD ... INTERVAL syntax. The CTE, join, aggregation, aliases, and grouping otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1097_game-play-analysis-v/variants/optimal/alignment_review.json) |
| 1107 | New Users Daily Count | `mysql` | `sql_dialect` | inclusive 90-day date filter | MySQL filters DATEDIFF('2019-06-30', login_date) BETWEEN 0 AND 90. | SQLite filters login_date between date('2019-06-30', '-90 day') and '2019-06-30'. | SQLite has no MySQL DATEDIFF function. Both predicates select the same inclusive calendar-date interval; the first-login CTE, projection, aggregation, aliases, and grouping otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1107_new-users-daily-count/variants/optimal/alignment_review.json) |
| 1193 | Monthly Transactions I | `mysql` | `sql_dialect` | year-month extraction | MySQL projects and groups by DATE_FORMAT(trans_date, '%Y-%m'). | SQLite projects and groups by substr(trans_date, 1, 7) for the authored ISO date values. | SQLite does not implement MySQL DATE_FORMAT. The fixture contract stores dates as ISO YYYY-MM-DD text, so the substring is the exact equivalent; every projection, aggregate, alias, grouping key, and ordering clause otherwise matches. | [alignment_review.json](../../dsa/leetcode/1193_monthly-transactions-i/variants/optimal/alignment_review.json) |
| 1205 | Monthly Transactions II | `mysql` | `sql_dialect` | year-month extraction | MySQL uses DATE_FORMAT(date_value, '%Y-%m') in both event branches. | SQLite uses substr(date_value, 1, 7) for the authored ISO dates. | SQLite does not implement MySQL DATE_FORMAT. Both branches, their UNION ALL, all six aggregate fields, grouping, aliases, and ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1205_monthly-transactions-ii/variants/optimal/alignment_review.json) |
| 1211 | Queries Quality and Percentage | `mysql` | `sql_dialect` | non-integral rating division | MySQL evaluates rating / position with its native division semantics. | SQLite promotes the numerator with 1.0 * rating / position. | SQLite divides two integer operands as integers, which would truncate the quality ratio. The explicit real promotion preserves MySQL's value; the poor-query expression, both aggregates, rounding, aliases, and grouping otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1211_queries-quality-and-percentage/variants/optimal/alignment_review.json) |
| 1225 | Report Contiguous Dates | `mysql` | `sql_dialect` | row-number date subtraction | MySQL forms the gaps-and-islands key with DATE_SUB(period_date, INTERVAL ROW_NUMBER() ... DAY). | SQLite formats the row number as a negative-day modifier and passes it to DATE(period_date, PRINTF(...)). | SQLite cannot parse MySQL's DATE_SUB ... INTERVAL syntax. The periods CTE, window partition and ordering, island grouping, projections, and final ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1225_report-contiguous-dates/variants/optimal/alignment_review.json) |
| 1251 | Average Selling Price | `mysql` | `sql_dialect` | weighted-average division | MySQL divides SUM(price * units) by SUM(units) with its native decimal division behavior. | SQLite multiplies the numerator by 1.0 before division. | SQLite truncates division when both operands are integers. The explicit real promotion preserves the weighted average; joins, date bounds, aggregates, fallback, rounding, aliases, and grouping otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1251_average-selling-price/variants/optimal/alignment_review.json) |
| 1384 | Total Sales Amount by Year | `mysql` | `sql_dialect` | inclusive overlap-day calculation | MySQL computes each yearly overlap with DATEDIFF(LEAST(period_end, year_end), GREATEST(period_start, year_start)) + 1. | SQLite computes the same inclusive difference with julianday(MIN(period_end, year_end)) - julianday(MAX(period_start, year_start)) + 1 and casts it to an integer. | SQLite has no MySQL DATEDIFF, LEAST, or GREATEST functions. The three yearly UNION ALL branches, overlap predicates, sales multiplication, aggregation, aliases, grouping, and ordering otherwise follow the Accepted query exactly. | [alignment_review.json](../../dsa/leetcode/1384_total-sales-amount-by-year/variants/optimal/alignment_review.json) |
| 1454 | Active Users | `mysql` | `sql_dialect` | consecutive-date streak key | MySQL subtracts each partitioned ROW_NUMBER from login_date with DATE_SUB and INTERVAL DAY. | SQLite subtracts the same ROW_NUMBER from julianday(login_date). | SQLite cannot parse MySQL's DATE_SUB ... INTERVAL syntax. The distinct-login input, partition and ordering, island grouping, five-day threshold, account join, projections, and final order otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1454_active-users/variants/optimal/alignment_review.json) |
| 1479 | Sales by Day of the Week | `mysql` | `sql_dialect` | weekday extraction | MySQL uses WEEKDAY(order_date), whose integer range starts with Monday at zero. | SQLite uses strftime('%w', order_date), whose text range starts with Sunday at zero, so each CASE uses the corresponding shifted literal. | SQLite does not provide MySQL's WEEKDAY function. All seven conditional sums, item left join, category grouping, projections, aliases, and ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1479_sales-by-day-of-the-week/variants/optimal/alignment_review.json) |
| 1484 | Group Sold Products By The Date | `mysql` | `sql_dialect` | ordered distinct string aggregation | MySQL applies DISTINCT and ORDER BY inside GROUP_CONCAT while COUNT independently counts distinct products. | SQLite first selects distinct date-product pairs in product order, then applies COUNT(*) and GROUP_CONCAT to that deduplicated relation. | SQLite's GROUP_CONCAT syntax cannot combine MySQL's DISTINCT, aggregate-local ORDER BY, and SEPARATOR clauses. The derived relation preserves the same deduplication and ordering before the same per-date count and concatenation. | [alignment_review.json](../../dsa/leetcode/1484_group-sold-products-by-the-date/variants/optimal/alignment_review.json) |
| 1501 | Countries You Can Safely Invest In | `mysql` | `sql_dialect` | country-code prefix extraction | MySQL extracts the first three phone-number characters with LEFT(phone_number, 3). | SQLite extracts the same prefix with SUBSTR(phone_number, 1, 3). | SQLite has no LEFT string function. The endpoint expansion, person and country joins, grouping, global-average comparison, projection, and ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1501_countries-you-can-safely-invest-in/variants/optimal/alignment_review.json) |
| 1517 | Find Users With Valid E-Mails | `mysql` | `sql_dialect` | case-sensitive full-address validation | MySQL uses REGEXP_LIKE with an anchored, case-sensitive regular expression for the prefix alphabet and exact @leetcode.com suffix. | SQLite combines GLOB for the leading letter and exact suffix with a second GLOB exclusion over the local part's permitted characters. | The bundled SQLite runtime has no REGEXP_LIKE operator. Both predicates accept exactly the same address language while preserving the same one-table filter and projections. | [alignment_review.json](../../dsa/leetcode/1517_find-users-with-valid-e-mails/variants/optimal/alignment_review.json) |
| 1543 | Fix Product Name Format | `mysql` | `sql_dialect` | sale-month formatting | MySQL derives the YYYY-MM grouping key with DATE_FORMAT(sale_date, '%Y-%m'). | SQLite derives the identical key with strftime('%Y-%m', sale_date). | SQLite does not implement MySQL's DATE_FORMAT function. Product normalization, grouping expressions, count, aliases, and ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1543_fix-product-name-format/variants/optimal/alignment_review.json) |
| 1565 | Unique Orders and Customers Per Month | `mysql` | `sql_dialect` | order-month formatting | MySQL derives the YYYY-MM projection and grouping key with DATE_FORMAT(order_date, '%Y-%m'). | SQLite derives the identical projection and grouping key with strftime('%Y-%m', order_date). | SQLite does not implement MySQL's DATE_FORMAT function. The invoice filter, order count, distinct-customer count, aliases, and grouping otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1565_unique-orders-and-customers-per-month/variants/optimal/alignment_review.json) |
| 1635 | Hopper Company Queries I | `mysql` | `sql_dialect` | calendar extraction and month-end cutoff | MySQL uses YEAR, MONTH, and DATE_ADD with an INTERVAL expression. | SQLite uses strftime for year/month extraction and date plus printf for the same relative month cutoff. | SQLite does not implement MySQL's YEAR, MONTH, or DATE_ADD syntax. The recursive month spine, correlated driver count, ride and acceptance joins, grouping, aliases, and ordering otherwise match. | [alignment_review.json](../../dsa/leetcode/1635_hopper-company-queries-i/variants/optimal/alignment_review.json) |
| 1645 | Hopper Company Queries II | `mysql` | `sql_dialect` | 1. relative month dates<br>2. percentage division | 1. MySQL constructs the monthly bounds with DATE_ADD and INTERVAL.<br>2. MySQL's numeric rules make the literal 100 produce the required fractional percentage. | 1. SQLite constructs the identical bounds with date and printf modifiers.<br>2. SQLite uses 100.0 to prevent integer division before rounding. | 1. SQLite does not implement MySQL's DATE_ADD or INTERVAL syntax. Both queries use the same two correlated counts and the same half-open month boundaries.<br>2. The real-valued literal is required only by SQLite's arithmetic semantics; it does not alter the formula or data flow. | [alignment_review.json](../../dsa/leetcode/1645_hopper-company-queries-ii/variants/optimal/alignment_review.json) |
| 1651 | Hopper Company Queries III | `mysql` | `sql_dialect` | request year and month extraction | MySQL selects 2020 requests with YEAR and MONTH. | SQLite extracts the same year and numeric month with strftime and CAST. | SQLite does not implement MySQL's YEAR or MONTH functions. The recursive month spine, monthly aggregation, forward window averages, filtering, aliases, and ordering otherwise match. | [alignment_review.json](../../dsa/leetcode/1651_hopper-company-queries-iii/variants/optimal/alignment_review.json) |
| 1667 | Fix Names in a Table | `mysql` | `sql_dialect` | substring extraction and concatenation | MySQL uses CONCAT, LEFT, and SUBSTRING. | SQLite uses the concatenation operator and SUBSTR for the same initial and suffix slices. | SQLite does not implement MySQL's CONCAT or LEFT functions. Projection columns, case conversion, row preservation, aliases, and ordering otherwise match. | [alignment_review.json](../../dsa/leetcode/1667_fix-names-in-a-table/variants/optimal/alignment_review.json) |
| 1683 | Invalid Tweets | `mysql` | `sql_dialect` | character-length function | MySQL counts characters with CHAR_LENGTH. | SQLite counts characters with LENGTH. | SQLite does not implement MySQL's CHAR_LENGTH function. The selected column, strict threshold predicate, and unordered result semantics otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1683_invalid-tweets/variants/optimal/alignment_review.json) |
| 1709 | Biggest Window Between Visits | `mysql` | `sql_dialect` | whole-day date difference | MySQL computes each visit gap with DATEDIFF. | SQLite subtracts the two julianday values and casts the maximum whole-day gap to INTEGER. | SQLite does not implement MySQL's DATEDIFF function. The LEAD window, 2021-01-01 terminal boundary, per-user maximum, grouping, aliases, and unordered result semantics otherwise match. | [alignment_review.json](../../dsa/leetcode/1709_biggest-window-between-visits/variants/optimal/alignment_review.json) |
| 1939 | Users That Actively Request Confirmation Messages | `mysql` | `sql_dialect` | inclusive 24-hour timestamp comparison | MySQL uses TIMESTAMPDIFF in seconds and compares the result with 86400. | SQLite compares the later timestamp with datetime(previous_time, '+24 hours'). | SQLite does not implement MySQL's TIMESTAMPDIFF function. The LAG window, user partition, chronological ordering, inclusive boundary, DISTINCT projection, and unordered result semantics otherwise match. | [alignment_review.json](../../dsa/leetcode/1939_users-that-actively-request-confirmation-messages/variants/optimal/alignment_review.json) |
| 1965 | Employees With Missing Information | `mysql` | `sql_dialect` | compound-query output alias | MySQL resolves ORDER BY employee_id from the unaliased qualified projections in both UNION branches. | SQLite requires each qualified projection to expose the common result-column name explicitly with AS employee_id. | The explicit aliases are required for SQLite to resolve the compound query's ORDER BY term. The two anti-joins, UNION duplicate elimination, filters, result ordering, and complexity otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/1965_employees-with-missing-information/variants/optimal/alignment_review.json) |
| 2118 | Build the Equation | `mysql` | `sql_dialect` | ordered string aggregation and concatenation syntax | MySQL uses CONCAT, IF, and GROUP_CONCAT with inline ORDER BY and SEPARATOR clauses. | SQLite uses CASE, the \|\| operator, GROUP_CONCAT's separator argument, and an ordered subquery. | SQLite does not implement MySQL's CONCAT, IF, or GROUP_CONCAT clause syntax. Both queries format one term per input row, order terms by descending power, concatenate them without a separator, and append '=0'. | [alignment_review.json](../../dsa/leetcode/2118_build-the-equation/variants/optimal/alignment_review.json) |
| 2153 | The Number of Passengers in Each Bus II | `mysql` | `sql_dialect` | two-value minimum expression | MySQL uses LEAST(capacity, waiting passengers) in both the recursive base and transition. | SQLite expresses the same two-value minimum with CASE because it has no LEAST function. | The ordered-bus CTE, cumulative arrival count, recursive carried departure total, capacity cap, output columns, and ordering are otherwise identical. | [alignment_review.json](../../dsa/leetcode/2153_the-number-of-passengers-in-each-bus-ii/variants/optimal/alignment_review.json) |
| 2175 | The Change in Global Rankings | `mysql` | `sql_dialect` | integer cast type | MySQL casts window ranks with AS SIGNED before subtraction. | SQLite casts the same window ranks with AS INTEGER. | SIGNED is MySQL-specific, while INTEGER is SQLite's corresponding integer cast. The two ranking CTEs, row ordering, join, names, and subtraction are identical. | [alignment_review.json](../../dsa/leetcode/2175_the-change-in-global-rankings/variants/optimal/alignment_review.json) |
| 2199 | Finding the Topic of Each Post | `mysql` | `sql_dialect` | whole-word match and ordered distinct aggregation | MySQL uses CONCAT plus LIKE and GROUP_CONCAT with DISTINCT, inline ORDER BY, and SEPARATOR clauses. | SQLite uses \|\| plus INSTR, then explicit distinct and ordered CTEs before GROUP_CONCAT's separator argument. | SQLite lacks MySQL's CONCAT and ordered-DISTINCT GROUP_CONCAT syntax. Both plans perform a case-insensitive space-bounded keyword match, deduplicate topic ids, sort them ascending, aggregate with commas, retain unmatched posts, and emit 'Ambiguous!'. | [alignment_review.json](../../dsa/leetcode/2199_finding-the-topic-of-each-post/variants/optimal/alignment_review.json) |
| 2205 | The Number of Users That Are Eligible for Discount | `mysql` | `platform_signature`<br>`app_execution_adapter`<br>`sql_dialect` | parameterized SQL function interface | LeetCode requires a MySQL getUserIDs(startDate, endDate, minAmount) stored function that returns one integer. | The SQLite harness supplies those three arguments as one Parameters row and executes the function's SELECT body directly. | SQLite does not support creating a MySQL stored function from submitted SQL. The app adapter changes only parameter transport and the scalar result column; the inclusive date filter, amount threshold, distinct-user count, and scan complexity match. | [alignment_review.json](../../dsa/leetcode/2205_the-number-of-users-that-are-eligible-for-discount/variants/optimal/alignment_review.json) |
| 2228 | Users With Two Purchases Within Seven Days | `mysql` | `sql_dialect` | date-distance expression | MySQL uses DATEDIFF(purchase_date, previous_date). | SQLite subtracts julianday(previous_date) from julianday(purchase_date). | SQLite does not implement MySQL's DATEDIFF. The LAG partition and ordering, derived table, inclusive seven-day comparison, DISTINCT projection, and result ordering are identical. | [alignment_review.json](../../dsa/leetcode/2228_users-with-two-purchases-within-seven-days/variants/optimal/alignment_review.json) |
| 2230 | The Users That Are Eligible for Discount | `mysql` | `platform_signature`<br>`app_execution_adapter`<br>`sql_dialect` | parameterized stored-procedure interface | LeetCode requires a MySQL getUserIDs(startDate, endDate, minAmount) procedure whose SELECT returns the result set. | The SQLite harness supplies those arguments as one Parameters row and executes the procedure's SELECT body directly. | SQLite cannot create or invoke MySQL stored procedures. The app adapter changes only parameter transport; the inclusive timestamp filter, amount threshold, distinct projection, ordering, and complexity match. | [alignment_review.json](../../dsa/leetcode/2230_the-users-that-are-eligible-for-discount/variants/optimal/alignment_review.json) |
| 2292 | Products With Three or More Orders in Two Consecutive Years | `mysql` | `sql_dialect` | calendar-year extraction | MySQL extracts an integer year with YEAR(purchase_date). | SQLite extracts the year text with strftime('%Y', purchase_date) and casts it to INTEGER. | SQLite does not implement MySQL's YEAR function. The yearly grouping, minimum order count, consecutive-year self-join, distinct product projection, and complexity are otherwise identical. | [alignment_review.json](../../dsa/leetcode/2292_products-with-three-or-more-orders-in-two-consecutive-years/variants/optimal/alignment_review.json) |
| 2298 | Tasks Count in the Weekend | `mysql` | `sql_dialect` | weekday extraction and Boolean aggregation | MySQL's DAYOFWEEK yields Sunday=1 through Saturday=7, and Boolean predicates sum directly. | SQLite's strftime('%w') yields Sunday=0 through Saturday=6, and CASE converts each predicate to 1 or 0. | SQLite lacks DAYOFWEEK and does not use MySQL's Boolean-expression aggregation idiom. Both single-scan aggregates count Saturday/Sunday as weekend and Monday-Friday as working days. | [alignment_review.json](../../dsa/leetcode/2298_tasks-count-in-the-weekend/variants/optimal/alignment_review.json) |
| 2394 | Employees With Deductions | `mysql` | `sql_dialect` | rounded-up minute duration | MySQL computes seconds with TIMESTAMPDIFF and rounds seconds/60 upward with CEIL. | SQLite subtracts Unix seconds obtained with strftime('%s') and uses integer (seconds + 59) / 60. | SQLite does not implement TIMESTAMPDIFF. The per-log upward rounding, left join, employee grouping, zero-log handling, required-minute comparison, and output are identical. | [alignment_review.json](../../dsa/leetcode/2394_employees-with-deductions/variants/optimal/alignment_review.json) |
| 2474 | Customers With Strictly Increasing Purchases | `mysql` | `sql_dialect` | calendar-year extraction | MySQL groups orders by YEAR(order_date). | SQLite groups by the integer cast of strftime('%Y', order_date). | SQLite has no YEAR function. The yearly totals, consecutive-year comparison, customer qualification, grouping, and ordering otherwise match exactly. | [alignment_review.json](../../dsa/leetcode/2474_customers-with-strictly-increasing-purchases/variants/optimal/alignment_review.json) |
| 2504 | Concatenate the Name and the Profession | `mysql` | `sql_dialect` | string concatenation and first-character extraction | MySQL uses CONCAT and LEFT(profession, 1). | SQLite uses the \|\| operator and SUBSTR(profession, 1, 1). | SQLite does not implement MySQL's CONCAT or LEFT functions. Projection names, punctuation, one-character profession abbreviation, and descending person_id ordering are identical. | [alignment_review.json](../../dsa/leetcode/2504_concatenate-the-name-and-the-profession/variants/optimal/alignment_review.json) |
| 2688 | Find Active Users | `mysql` | `sql_dialect` | elapsed calendar days | MySQL uses DATEDIFF(created_at, previous_purchase). | SQLite subtracts the two julianday values. | SQLite does not implement MySQL's DATEDIFF function. The LAG window, user partition, chronological order, seven-day threshold, distinct projection, and resulting rows are otherwise identical. | [alignment_review.json](../../dsa/leetcode/2688_find-active-users/variants/optimal/alignment_review.json) |
| 2701 | Consecutive Transactions with Increasing Amounts | `mysql` | `sql_dialect` | consecutive-date detection | MySQL checks DATEDIFF(transaction_date, previous_date) = 1. | SQLite checks that the difference between the two julianday values equals 1. | SQLite does not implement MySQL's DATEDIFF function. Both forms use the same customer/date window order, increasing-amount condition, gap-and-island markers, grouping, minimum run length, and output ordering. | [alignment_review.json](../../dsa/leetcode/2701_consecutive-transactions-with-increasing-amounts/variants/optimal/alignment_review.json) |
| 2738 | Count Occurrences in Text | `mysql` | `sql_dialect` | trailing source comment | The Accepted MySQL source ends with a # comment after the final statement. | The SQLite source omits that non-executable comment. | The # comment marker is MySQL-specific and is rejected by SQLite. The two SELECT branches, LIKE patterns, UNION ALL, aliases, and results are otherwise textually identical. | [alignment_review.json](../../dsa/leetcode/2738_count-occurrences-in-text/variants/optimal/alignment_review.json) |
| 2752 | Customers with Maximum Number of Transactions on Consecutive Days | `mysql` | `sql_dialect` | date-to-day-number conversion | MySQL converts each transaction date with TO_DAYS. | SQLite converts julianday(transaction_date) to an integer. | SQLite does not implement MySQL's TO_DAYS function. Both conversions assign consecutive integer values to consecutive dates, so subtracting the same per-customer row number produces identical islands; all later grouping, ranking, and output logic is identical. | [alignment_review.json](../../dsa/leetcode/2752_customers-with-maximum-number-of-transactions-on-consecutive-days/variants/optimal/alignment_review.json) |
| 2783 | Flight Occupancy and Waitlist Analysis | `mysql` | `sql_dialect` | scalar minimum and maximum expressions | MySQL uses LEAST for booked passengers and GREATEST for the nonnegative waitlist. | SQLite expresses the same two bounds with CASE expressions. | The installed SQLite version does not provide MySQL's scalar LEAST and GREATEST functions. The flight/passenger left join, passenger count, capacity comparisons, grouping, aliases, and results are identical. | [alignment_review.json](../../dsa/leetcode/2783_flight-occupancy-and-waitlist-analysis/variants/optimal/alignment_review.json) |
| 2854 | Rolling Average Steps | `mysql` | `sql_dialect` | three-calendar-day window | MySQL uses a RANGE window with INTERVAL 2 DAY PRECEDING and retains windows containing three rows. | SQLite averages the current and two preceding rows, then uses LAG plus julianday subtraction to require a two-day span. | SQLite does not support a date INTERVAL in a RANGE frame. Because (user_id, steps_date) is unique, three ordered rows spanning exactly two days are exactly three consecutive calendar dates. Both queries partition and order identically, compute the same average, round to two decimals, and emit the same ordered columns. | [alignment_review.json](../../dsa/leetcode/2854_rolling-average-steps/variants/optimal/alignment_review.json) |
| 2889 | Reshape Data: Pivot | `python3` | `app_execution_adapter`<br>`platform_signature` | app entry point and DataFrame index serialization | LeetCode calls pivotTable(weather), whose exact body returns the pivoted DataFrame with month as its named index. | The identical pivotTable function is retained, while solve calls it and resets the named index so the JSON table result includes month as an ordinary output column. | The app runner invokes solve and serializes DataFrame columns rather than pandas indexes. reset_index changes only the transport representation; the pivot operation, row labels, city columns, values, ordering, and complexity remain anchored to the exact native function. | [alignment_review.json](../../dsa/leetcode/2889_reshape-data-pivot/variants/optimal/alignment_review.json) |
| 2893 | Calculate Orders Within Each Interval | `mysql` | `sql_dialect` | integer interval division | MySQL uses the DIV operator. | SQLite divides and casts the nonnegative quotient to INTEGER. | SQLite does not implement DIV. Both expressions map each legal minute to the same one-based six-minute interval; aggregation, aliases, grouping, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/2893_calculate-orders-within-each-interval/variants/optimal/alignment_review.json) |
| 2984 | Find Peak Calling Hours for Each City | `mysql` | `sql_dialect` | hour extraction | MySQL extracts the hour with HOUR(call_time). | SQLite extracts %H with strftime and casts it to INTEGER. | SQLite does not implement HOUR. Both queries group by the same city and numeric hour, count calls, apply the same rank, and return the same peak rows and ordering. | [alignment_review.json](../../dsa/leetcode/2984_find-peak-calling-hours-for-each-city/variants/optimal/alignment_review.json) |
| 2985 | Calculate Compressed Mean | `mysql` | `sql_dialect` | non-integer weighted division | MySQL's aggregate quotient preserves the required fractional value. | SQLite multiplies the numerator by 1.0 before division. | SQLite otherwise performs integer division when both aggregate operands are integers. The weighted sum, occurrence sum, two-decimal rounding, alias, and result are identical. | [alignment_review.json](../../dsa/leetcode/2985_calculate-compressed-mean/variants/optimal/alignment_review.json) |
| 2991 | Top Three Wineries | `mysql` | `sql_dialect` | winery label concatenation | MySQL builds each label with CONCAT. | SQLite builds the same label with the \|\| operator. | SQLite does not implement MySQL's CONCAT function. Totals, per-country ranking, tie-breaking, conditional pivot, fallback labels, aliases, and country ordering are identical. | [alignment_review.json](../../dsa/leetcode/2991_top-three-wineries/variants/optimal/alignment_review.json) |
| 2993 | Friday Purchases I | `mysql` | `sql_dialect` | day-of-month and weekday extraction | MySQL uses DAY, DAYOFWEEK, and FLOOR. | SQLite uses strftime for the day fields and an INTEGER cast for the nonnegative quotient. | SQLite does not implement the MySQL date functions. Both forms select Fridays, derive the same one-based week of November, aggregate each date, and order by week identically. | [alignment_review.json](../../dsa/leetcode/2993_friday-purchases-i/variants/optimal/alignment_review.json) |
| 3050 | Pizza Toppings Cost Analysis | `mysql` | `sql_dialect` | pizza-name concatenation | MySQL joins the three topping names with CONCAT. | SQLite joins the same names and commas with \|\|. | SQLite does not implement MySQL's CONCAT function. The three ordered self-joins, cost sum, rounding, aliases, and result ordering are identical. | [alignment_review.json](../../dsa/leetcode/3050_pizza-toppings-cost-analysis/variants/optimal/alignment_review.json) |
| 3052 | Maximize Items | `mysql` | `sql_dialect` | whole-batch division | MySQL floors both capacity quotients with FLOOR. | SQLite casts the two nonnegative quotients to INTEGER. | The cast is SQLite's equivalent truncation for these nonnegative values. Batch aggregation, prime-first allocation, remaining capacity, item counts, labels, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3052_maximize-items/variants/optimal/alignment_review.json) |
| 3060 | User Activities within Time Bounds | `mysql` | `sql_dialect` | twelve-hour timestamp bound | MySQL adds INTERVAL 12 HOUR with DATE_ADD. | SQLite adds one half-day to the prior end's julianday value. | SQLite does not implement DATE_ADD with MySQL interval syntax. The prior-session window, inclusive twelve-hour comparison, distinct users, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3060_user-activities-within-time-bounds/variants/optimal/alignment_review.json) |
| 3061 | Calculate Trapping Rain Water | `mysql` | `sql_dialect` | scalar boundary minimum | MySQL selects the smaller boundary with LEAST. | SQLite uses its two-argument scalar MIN function. | SQLite does not implement LEAST. Both queries compute identical left and right running maxima and sum the same per-position trapped-water depth. | [alignment_review.json](../../dsa/leetcode/3061_calculate-trapping-rain-water/variants/optimal/alignment_review.json) |
| 3064 | Guess the Number Using Bitwise Questions I | `python3` | `app_execution_adapter`<br>`judge_environment`<br>`platform_signature` | hidden bit-count oracle | LeetCode calls findNumber with no arguments and injects commonSetBits(num). | solve receives the fixture's hidden n and constructs an explicit CommonSetBitsAPI callable before running the same thirty one-bit probes. | The app must serialize the hidden value to reproduce an interactive judge locally. Each query returns exactly (n & num).bit_count(); bit order, masks, answer updates, result, and complexity match the Accepted algorithm. | [alignment_review.json](../../dsa/leetcode/3064_guess-the-number-using-bitwise-questions-i/variants/optimal/alignment_review.json) |
| 3087 | Find Trending Hashtags | `mysql` | `sql_dialect` | single hashtag extraction | MySQL extracts the one legal hashtag with REGEXP_SUBSTR. | SQLite locates # with INSTR and slices through the next space with SUBSTR. | SQLite lacks the required built-in regular-expression substring function. Under the one-hashtag token contract, both extract the same token; February filtering, counting, tie-breaking, and top-three limit are identical. | [alignment_review.json](../../dsa/leetcode/3087_find-trending-hashtags/variants/optimal/alignment_review.json) |
| 3089 | Find Bursty Behavior | `mysql` | `sql_dialect` | seven-day range frame | MySQL orders by the date and uses RANGE INTERVAL 6 DAY PRECEDING. | SQLite orders by julianday(post_date) and uses the numeric RANGE 6 PRECEDING. | SQLite does not support MySQL interval syntax in a RANGE frame. Both frames cover the same inclusive seven calendar days; February filtering, weekly average, burst threshold, grouping, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3089_find-bursty-behavior/variants/optimal/alignment_review.json) |
| 3094 | Guess the Number Using Bitwise Questions II | `python3` | `app_execution_adapter`<br>`judge_environment`<br>`platform_signature` | stateful hidden-bit oracle | LeetCode calls findNumber with no arguments and injects the mutating commonBits(num) API. | solve receives initial n and constructs an explicit CommonBitsAPI callable with the same count-before-XOR state transition. | The serialized initial value is required to reproduce the interactive judge. The initial zero query, thirty one-bit queries, state transitions, comparisons, answer updates, result, and complexity match the Accepted algorithm. | [alignment_review.json](../../dsa/leetcode/3094_guess-the-number-using-bitwise-questions-ii/variants/optimal/alignment_review.json) |
| 3103 | Find Trending Hashtags II | `mysql` | `sql_dialect` | recursive hashtag tokenization | MySQL recursively requests successive # followed non-space matches with REGEXP_SUBSTR. | SQLite recursively splits each tweet into space-delimited words and retains words beginning with #. | SQLite lacks the required regular-expression substring function. Under the source token contract, both emit every hashtag occurrence including punctuation through the next space; counting, tie-breaking, and the top-three limit are identical. | [alignment_review.json](../../dsa/leetcode/3103_find-trending-hashtags-ii/variants/optimal/alignment_review.json) |
| 3124 | Find Longest Calls | `mysql` | `sql_dialect` | duration formatting | MySQL converts seconds with SEC_TO_TIME and formats the value with TIME_FORMAT. | SQLite formats the same hours, minutes, and seconds components with printf. | SQLite does not implement MySQL's SEC_TO_TIME or TIME_FORMAT functions. Ranking, row selection, output fields, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3124_find-longest-calls/variants/optimal/alignment_review.json) |
| 3126 | Server Utilization Time | `mysql` | `sql_dialect` | timestamp arithmetic and flooring | MySQL converts each timestamp to epoch seconds with TIMESTAMPDIFF and floors the summed day count. | SQLite obtains the same epoch seconds with strftime and truncates the nonnegative day count with an INTEGER cast. | SQLite lacks TIMESTAMPDIFF and FLOOR in the bundled runtime. The signed event sum and division by 86400 are otherwise identical; total uptime is nonnegative, so truncation equals floor. | [alignment_review.json](../../dsa/leetcode/3126_server-utilization-time/variants/optimal/alignment_review.json) |
| 3150 | Invalid Tweets II | `mysql` | `sql_dialect` | character-count function | MySQL measures content and replacement lengths with CHAR_LENGTH. | SQLite measures the same text values with LENGTH. | SQLite does not expose CHAR_LENGTH. For the problem's text contract, SQLite LENGTH counts characters and preserves all three predicates and output ordering. | [alignment_review.json](../../dsa/leetcode/3150_invalid-tweets-ii/variants/optimal/alignment_review.json) |
| 3156 | Employee Task Duration and Concurrent Tasks | `mysql` | `sql_dialect` | interval duration and flooring | MySQL calculates interval seconds with TIMESTAMPDIFF and floors the summed hour count. | SQLite subtracts strftime epoch seconds and truncates the nonnegative hour count with an INTEGER cast. | SQLite lacks TIMESTAMPDIFF and FLOOR in the bundled runtime. Event construction, coalescing, sweep-line windows, concurrency maximum, grouping, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3156_employee-task-duration-and-concurrent-tasks/variants/optimal/alignment_review.json) |
| 3166 | Calculate Parking Fees and Duration | `mysql` | `sql_dialect` | duration and numeric division | MySQL obtains seconds with TIMESTAMPDIFF and naturally evaluates the hourly-rate division as noninteger arithmetic. | SQLite subtracts strftime epoch seconds and uses 3600.0 to prevent integer division. | SQLite lacks TIMESTAMPDIFF and its numeric affinity requires an explicit real operand here. The per-lot totals, ranking, car totals, rounding, join, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3166_calculate-parking-fees-and-duration/variants/optimal/alignment_review.json) |
| 3172 | Second Day Verification | `mysql` | `sql_dialect` | next-day date predicate | MySQL checks DATEDIFF(action_date, signup_date) = 1. | SQLite compares the action date with DATE(signup_date, '+1 day'). | SQLite does not implement DATEDIFF. Both express the same calendar-day condition; join, verification filter, distinct projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3172_second-day-verification/variants/optimal/alignment_review.json) |
| 3198 | Find Cities in Each State | `mysql` | `sql_dialect` | ordered string aggregation | MySQL places the city order and separator inside GROUP_CONCAT. | SQLite orders the source rows in a subquery and supplies ', ' as GROUP_CONCAT's second argument. | SQLite does not support MySQL's GROUP_CONCAT ORDER BY ... SEPARATOR syntax. Both group once per state and concatenate cities in ascending order with the same delimiter. | [alignment_review.json](../../dsa/leetcode/3198_find-cities-in-each-state/variants/optimal/alignment_review.json) |
| 3204 | Bitwise User Permissions Analysis | `mysql` | `sql_dialect` | bitwise aggregation | MySQL reduces all permission masks with BIT_AND and BIT_OR aggregate functions. | SQLite assigns row numbers and performs the same AND/OR fold in a recursive CTE. | SQLite has scalar bitwise operators but no BIT_AND or BIT_OR aggregates. The recursive fold preserves the native reductions and returns the same two fields in linear time. | [alignment_review.json](../../dsa/leetcode/3204_bitwise-user-permissions-analysis/variants/optimal/alignment_review.json) |
| 3214 | Year on Year Growth Rate | `mysql` | `sql_dialect` | year extraction and percentage arithmetic | MySQL extracts YEAR directly and its division produces the fractional growth rate before multiplication by 100. | SQLite extracts and casts strftime('%Y') and uses 100.0 to force real division. | SQLite lacks YEAR and otherwise risks integer division. Yearly aggregation, LAG partitioning, output values, rounding, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3214_year-on-year-growth-rate/variants/optimal/alignment_review.json) |
| 3230 | Customer Purchasing Behavior Analysis | `mysql` | `sql_dialect` | loyalty-score division | MySQL evaluates SUM(amount) / 100 as noninteger arithmetic. | SQLite divides by 100.0 to force the same fractional result. | SQLite can perform integer division when both operands have integer affinity. All CTEs, aggregates, ranking logic, filters, output fields, and ordering are otherwise identical. | [alignment_review.json](../../dsa/leetcode/3230_customer-purchasing-behavior-analysis/variants/optimal/alignment_review.json) |
| 3268 | Find Overlapping Shifts II | `mysql` | `sql_dialect` | minute duration | MySQL calculates complete elapsed minutes with TIMESTAMPDIFF(MINUTE, event_time, next_time). | SQLite subtracts strftime epoch seconds and uses integer division by 60. | SQLite does not implement TIMESTAMPDIFF. Event construction, same-time ordering, cumulative active counts, pair counts, duration aggregation, grouping, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3268_find-overlapping-shifts-ii/variants/optimal/alignment_review.json) |
| 3328 | Find Cities in Each State II | `mysql` | `sql_dialect` | ordered concatenation and collation | MySQL orders city names inside GROUP_CONCAT and uses SEPARATOR ', '; its configured collation supplies dictionary-style comparison. | SQLite orders rows in a CTE using a case-insensitive, space-neutral key before GROUP_CONCAT(city, ', '). | SQLite lacks MySQL's inline GROUP_CONCAT ordering/separator syntax and does not share its default collation. The adapter reproduces the required city ordering; grouping, conditional count, eligibility filters, projection, and final ordering are unchanged. | [alignment_review.json](../../dsa/leetcode/3328_find-cities-in-each-state-ii/variants/optimal/alignment_review.json) |
| 3368 | First Letter Capitalization | `mysql` | `sql_dialect` | recursive text construction | MySQL seeds a CHAR buffer and appends each converted character with CONCAT. | SQLite seeds a TEXT buffer and appends with \|\|. | SQLite does not implement MySQL's CONCAT or CHAR type syntax. The recursive state, character positions, word-boundary rule, case conversion, terminal row, projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3368_first-letter-capitalization/variants/optimal/alignment_review.json) |
| 3374 | First Letter Capitalization II | `mysql` | `sql_dialect` | hyphenated-word predicate and text construction | MySQL isolates the current word with SUBSTRING_INDEX, validates it with REGEXP, and appends each converted character with CONCAT. | SQLite isolates the same word with SUBSTR and INSTR, checks the same one-hyphen ASCII-letter grammar with LENGTH, REPLACE, and GLOB, and appends with \|\|. | SQLite lacks SUBSTRING_INDEX, the required REGEXP operator, and CONCAT. Both queries retain the same character-by-character recursive state, word-start updates, capitalization decisions, terminal row, projection, and ordering. | [alignment_review.json](../../dsa/leetcode/3374_first-letter-capitalization-ii/variants/optimal/alignment_review.json) |
| 3384 | Team Dominance by Pass Success | `mysql` | `sql_dialect` | conditional expressions | MySQL uses IF for the half number and signed pass contribution. | SQLite expresses the same two conditions with CASE. | SQLite does not provide MySQL's IF scalar function. Joins, predicates, grouping, sums, aliases, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3384_team-dominance-by-pass-success/variants/optimal/alignment_review.json) |
| 3415 | Find Products with Three Consecutive Digits | `mysql` | `sql_dialect` | bounded three-digit match | MySQL uses a regular expression for exactly three digits bounded by nondigits or string edges. | SQLite pads both edges with a nondigit and uses a GLOB pattern with the same three digit and nondigit boundaries. | SQLite does not provide the required REGEXP operator. Projection, filtering semantics, and product ordering are identical. | [alignment_review.json](../../dsa/leetcode/3415_find-products-with-three-consecutive-digits/variants/optimal/alignment_review.json) |
| 3436 | Find Valid Emails | `mysql` | `sql_dialect` | email grammar validation | MySQL validates the complete address with one regular expression. | SQLite parses the at-sign, local part, domain, and .com suffix and enforces the same character classes with GLOB. | SQLite does not provide the required REGEXP operator. The adapter implements the same anchored grammar, projection, and user ordering. | [alignment_review.json](../../dsa/leetcode/3436_find-valid-emails/variants/optimal/alignment_review.json) |
| 3451 | Find Invalid IP Addresses | `mysql` | `sql_dialect` | IPv4 component extraction | MySQL extracts four dot-delimited components with SUBSTRING_INDEX and detects leading zeros with REGEXP. | SQLite recursively emits dot-delimited components, then aggregates their count, numeric bounds, and leading-zero flags. | SQLite lacks SUBSTRING_INDEX and the required REGEXP operator. Both classify the same invalid conditions, count duplicate invalid addresses, and use the same result ordering. | [alignment_review.json](../../dsa/leetcode/3451_find-invalid-ip-addresses/variants/optimal/alignment_review.json) |
| 3465 | Find Products with Valid Serial Numbers | `mysql` | `sql_dialect` | case-sensitive serial-token search | MySQL uses REGEXP_LIKE with a case-sensitive word-boundary pattern for SNdddd-dddd. | SQLite enumerates candidate offsets and checks the same prefix, digit groups, hyphen, and alphanumeric-or-underscore boundaries with SUBSTR and GLOB. | SQLite lacks REGEXP_LIKE. DISTINCT collapses multiple matches in one description, preserving the native row filter, projection, and ordering. | [alignment_review.json](../../dsa/leetcode/3465_find-products-with-valid-serial-numbers/variants/optimal/alignment_review.json) |
| 3564 | Seasonal Sales Analysis | `mysql` | `sql_dialect` | season extraction | MySQL maps the numeric month with MONTH, MOD, integer DIV, and ELT. | SQLite extracts the month with strftime, casts it to an integer, and selects the same season with CASE membership tests. | MySQL's ELT and DIV functions are unavailable in SQLite. Both expressions implement the identical December-through-February Winter mapping and leave the join, aggregation, ranking, tie-breakers, projection, and ordering unchanged. | [alignment_review.json](../../dsa/leetcode/3564_seasonal-sales-analysis/variants/optimal/alignment_review.json) |
| 3586 | Find COVID Recovery Patients | `mysql` | `sql_dialect` | recovery-day calculation | MySQL calculates the day difference with DATEDIFF. | SQLite subtracts the two julianday values and casts the integral result. | SQLite does not implement DATEDIFF. The status-event CTEs, first-positive/first-negative pairing, patient join, output fields, filters, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3586_find-covid-recovery-patients/variants/optimal/alignment_review.json) |
| 3590 | Kth Smallest Path XOR Sum | `python3` | `app_execution_adapter` | ordered-set dependency | LeetCode supplies sortedcontainers.SortedList for membership, ordered iteration, insertion, length, and kth indexing. | solve.py defines an explicit local SortedList-compatible ordered set backed by a deterministic treap, then runs the same Accepted solution body. | The offline app runtime does not bundle the third-party sortedcontainers package. The adapter preserves the exact interface and logarithmic operations; tree traversal, DSU-on-tree merges, query grouping, answers, and complexity match the Accepted algorithm. | [alignment_review.json](../../dsa/leetcode/3590_kth-smallest-path-xor-sum/variants/optimal/alignment_review.json) |
| 3601 | Find Drivers with Improved Fuel Efficiency | `mysql` | `sql_dialect` | trip-month extraction | MySQL extracts the month with MONTH(trip_date). | SQLite extracts and integer-casts strftime('%m', trip_date). | SQLite does not implement MONTH. The half-year conditional averages, improvement predicate, driver join, rounding, projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3601_find-drivers-with-improved-fuel-efficiency/variants/optimal/alignment_review.json) |
| 3611 | Find Overbooked Employees | `mysql` | `sql_dialect` | Monday-based week key | MySQL groups meeting dates with YEARWEEK(meeting_date, 1). | SQLite maps each meeting date to that week's Monday using strftime and date modifiers. | SQLite does not implement YEARWEEK. Both aggregate the same employee/week groups, filter over-20-hour weeks, count qualifying weeks, join employees, and order the result identically. | [alignment_review.json](../../dsa/leetcode/3611_find-overbooked-employees/variants/optimal/alignment_review.json) |
| 3617 | Find Students with Study Spiral Pattern | `mysql` | `sql_dialect` | day difference and modulo | MySQL uses DATEDIFF for consecutive dates and MOD for cycle positions. | SQLite subtracts julianday values and uses the % operator. | SQLite lacks DATEDIFF and MySQL's MOD function form. Session ordering, cycle statistics, subject comparison, qualification rules, joins, projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3617_find-students-with-study-spiral-pattern/variants/optimal/alignment_review.json) |
| 3626 | Find Stores with Inventory Imbalance | `mysql` | `sql_dialect` | inventory-ratio division | MySQL evaluates cheap.quantity / expensive.quantity as noninteger arithmetic. | SQLite multiplies the numerator by 1.0 to force the same fractional quotient. | SQLite otherwise applies integer division to integer-affinity values. Ranking, joins, eligibility conditions, rounding, aliases, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3626_find-stores-with-inventory-imbalance/variants/optimal/alignment_review.json) |
| 3657 | Find Loyal Customers | `mysql` | `sql_dialect` | activity-span calculation | MySQL calculates the date span with DATEDIFF(MAX(...), MIN(...)). | SQLite subtracts the corresponding julianday values. | SQLite does not implement DATEDIFF. Customer grouping, purchase threshold, refund-rate inequality, projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3657_find-loyal-customers/variants/optimal/alignment_review.json) |
| 3673 | Find Zombie Sessions | `mysql` | `sql_dialect` | session duration in complete minutes | MySQL uses TIMESTAMPDIFF(MINUTE, MIN(timestamp), MAX(timestamp)). | SQLite subtracts epoch seconds and performs integer division by 60. | SQLite does not implement TIMESTAMPDIFF. Aggregation, scroll/click/purchase predicates, output fields, grouping, aliases, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3673_find-zombie-sessions/variants/optimal/alignment_review.json) |
| 3716 | Find Churn Risk Customers | `mysql` | `sql_dialect` | subscriber-day calculation | MySQL uses DATEDIFF for both the projected subscription length and the 60-day filter. | SQLite subtracts julianday values and casts the projected integral result. | SQLite does not implement DATEDIFF. Event-history ranking, downgrade evidence, current-plan filters, amount comparison, projection, and ordering are identical. | [alignment_review.json](../../dsa/leetcode/3716_find-churn-risk-customers/variants/optimal/alignment_review.json) |
| 3808 | Find Emotionally Consistent Users | `mysql` | `sql_dialect` | decimal division | MySQL's / operator divides the two integer counts as a decimal value before ROUND is applied. | SQLite multiplies the numerator by 1.0 so / performs real rather than integer division before the same ROUND call. | The cast is the SQLite-compatible expression of the identical reaction-count ratio; every CTE, predicate, projection, alias, and ordering rule remains the same. | [alignment_review.json](../../dsa/leetcode/3808_find-emotionally-consistent-users/variants/optimal/alignment_review.json) |
| 3832 | Find Users with Persistent Behavior Patterns | `mysql` | `sql_dialect` | consecutive-date island key | MySQL groups each numbered row by DATE_SUB(action_date, INTERVAL day_number DAY). | SQLite groups each numbered row by julianday(action_date) - day_number. | Both subtract the row ordinal from the calendar date to produce one constant key per consecutive-day streak; SQLite has no MySQL DATE_SUB ... INTERVAL syntax. | [alignment_review.json](../../dsa/leetcode/3832_find-users-with-persistent-behavior-patterns/variants/optimal/alignment_review.json) |
