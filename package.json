{
  "name": "coden",
  "version": "0.1.0",
  "private": true,
  "description": "cOde(n) — root scripts for the web rebuild (2026-06-07).",
  "scripts": {
    "build:web": "npm run build --prefix web",
    "build:electron": "npm run build --prefix electron",
    "start:electron": "npm start --prefix electron",
    "dev": "npm run build:web && npm run start:electron",
    "build:app": ".venv/Scripts/python.exe build_app.py",
    "test:engine": ".venv/Scripts/python.exe -m unittest discover -s tests",
    "test:server": ".venv/Scripts/python.exe -m unittest discover -s server/tests -t .",
    "test:web": "npm run typecheck --prefix web",
    "test": "npm run test:engine && npm run test:server && npm run test:web"
  }
}
