"""Solution for hash_05: Count Distinct in Window.

For each window of size k in arr, return the number of
distinct elements. Sliding window + count map: O(n) total.
Source: https://www.geeksforgeeks.org/count-distinct-elements-in-every-window-of-size-k/

Inputs passed to solve():
    arr: list of n integers (small value range).
    k: window size (1..4 in the setup).
    n: length of arr.

Goal:
    a list of n - k + 1 distinct counts (one per window).

Samples:
Sample 1 input:  arr = [1, 2, 1, 3, 4, 2, 3], k = 4, n = 7
Sample 1 output: [3, 4, 4, 3]

Sample 2 input:  arr = [1, 1, 1, 1], k = 2, n = 4
Sample 2 output: [1, 1, 1]


"""

def solve(arr, k, n):
    # Write your code here.
    return None
