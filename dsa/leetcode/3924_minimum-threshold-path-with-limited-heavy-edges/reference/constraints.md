## Constraints

- $1 \le n \le 1000$
- $0 \le \texttt{edges.length} \le 1000$
- `edges[i].length == 3`
- $0 \le u_i, v_i < n$
- $1 \le w_i \le 10^9$
- $0 \le \texttt{source}, \texttt{target} < n$
- $0 \le k \le \texttt{edges.length}$
