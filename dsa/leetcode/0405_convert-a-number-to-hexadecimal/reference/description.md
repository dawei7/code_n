## Description

Given a signed 32-bit integer `num`, return its hexadecimal representation as a string. Negative values must be represented using 32-bit two's-complement form.

Use lowercase letters, and omit leading zeroes except when the value itself is zero.
