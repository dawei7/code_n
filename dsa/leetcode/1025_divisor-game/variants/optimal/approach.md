## General
Given Alice and Bob take turns playing a game, with Alice starting first, the algorithm solves **Divisor Game** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
