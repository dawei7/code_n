## Friendship Table

| Column Name | Type |
|---|---|
| `user1_id` | int |
| `user2_id` | int |

The pair `(user1_id, user2_id)` is the composite primary key, so each ordered identifier pair is unique. Every row records a friendship relation between its two users.
