## Constraints

- $k = \lvert\texttt{lists}\rvert$
- $0 \le k \le 10^4$
- $0 \le \lvert\texttt{lists[i]}\rvert \le 500$
- $-10^4 \le \texttt{lists[i][j]} \le 10^4$
- Every `lists[i]` is sorted in ascending order.
- The total number of nodes across all lists is at most $10^4$.
