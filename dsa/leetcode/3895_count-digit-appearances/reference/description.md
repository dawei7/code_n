## Description

You are given an integer array `nums` and one integer `digit`. Examine the ordinary decimal representation of every element in `nums` and count every position whose digit equals `digit`.

Return the total number of matching positions across the entire array. Repeated copies inside one number each contribute separately, as do matches belonging to different array elements. Every element is positive, so its representation has neither a sign nor a standalone representation of the number zero.
