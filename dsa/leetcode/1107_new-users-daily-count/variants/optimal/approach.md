## General
Executes a SQL query for **New Users Daily Count** using Common Table Expressions (CTEs), GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(N \log N)$ — Operation count bound.
- **Space Complexity**: $O(U)$ — Auxiliary memory allocation bound.
