## Constraints

- $1 \le \text{s.length} \le 100$.
- `s` consists of digits `'0'` through `'9'`, operators `'+'`, `'-'`, `'*'`, `'/'`, and parentheses `'('`, `')'`.
- `s` is a valid infix arithmetic expression.
