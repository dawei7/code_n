## Description

Given the `root` of a binary tree, view the tree as though you were standing to its right. At every depth, only the node that is not obscured by another node farther to the right is visible.

Return those visible node values from the top level down to the lowest occupied level.
