## General
Given an array of integers `nums` containing $n + 1$ integers where each integer is in the range `[1, n]` inclusive, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
