## General
Algorithm uses binary search over sorted domain. Edge cases: handles empty/null inputs via early return guards.

## Complexity detail
- **Time Complexity**: $O(Q log U)$ — Operation count bound.
- **Space Complexity**: $O(Q log U)$ — Auxiliary memory allocation bound.
