## General
Given an **empty** set of intervals, implement a data structure that can:, the algorithm executes binary search over the search space to achieve logarithmic reduction. Edge case handling: guards against empty/null inputs via early returns.

## Complexity detail
- **Time Complexity**: $O(Q log U)$ — Operation count bound.
- **Space Complexity**: $O(Q log U)$ — Auxiliary memory allocation bound.
