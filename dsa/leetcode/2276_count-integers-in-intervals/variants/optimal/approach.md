## General
Given an **empty** set of intervals, implement a data structure that can:, the algorithm executes binary search over the search space to achieve logarithmic reduction. It utilizes a hash map (`dict`) to store element values and their indices/frequencies for $O(1)$ fast access.

## Complexity detail
- **Time Complexity**: $O(Q log U)$ — Operation count bound.
- **Space Complexity**: $O(Q log U)$ — Auxiliary memory allocation bound.
