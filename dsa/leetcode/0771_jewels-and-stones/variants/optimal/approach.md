## General
Algorithm uses single-pass sequential scanning.

## Complexity detail
- **Time Complexity**: $O(j+s)$ — Operation count bound.
- **Space Complexity**: $O(j)$ — Auxiliary memory allocation bound.
