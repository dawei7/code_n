## General
Given two positive integers `n` and `k`, the binary string $S_{n}$ is formed as follows:, the algorithm solves **Find Kth Bit in Nth Binary String** directly. Key operations include bitwise operators (`&`, `|`, `^`, `<<`, `>>`) for fast bitmask state updates.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
