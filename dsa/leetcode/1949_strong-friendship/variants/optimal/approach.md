## General
Executes a SQL query for **Strong Friendship** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O(E+W\log W)$ — Operation count bound.
- **Space Complexity**: $O(E+W)$ — Auxiliary memory allocation bound.
