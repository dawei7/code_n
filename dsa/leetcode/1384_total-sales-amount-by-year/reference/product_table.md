## Product Table

| Column Name | Type |
|---|---|
| `product_id` | int |
| `product_name` | varchar |

`product_id` is the table's primary key, so it identifies exactly one product row. `product_name` stores that product's name.
