## General
Executes a SQL query for **Employee Task Duration and Concurrent Tasks** using Common Table Expressions (CTEs), GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(n log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
