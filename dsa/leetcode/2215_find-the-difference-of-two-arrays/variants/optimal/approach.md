## General
Implements the optimal algorithm for **Find the Difference of Two Arrays** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n + m)$ — Operation count bound.
- **Space Complexity**: $O(n + m)$ — Auxiliary memory allocation bound.
