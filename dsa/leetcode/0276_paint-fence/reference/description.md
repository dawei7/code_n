## Description

A fence has `n` posts and `k` available colors. Paint the fence under both of these rules:

- Every post receives exactly one color.
- No sequence of three or more consecutive posts may use the same color.

Given `n` and `k`, return the number of valid ways to paint all the posts.
