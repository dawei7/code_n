## General
Given two strings `s` and `part`, perform the following operation on `s` until **all** occurrences of the substring `part` are removed:, the algorithm solves **Remove All Occurrences of a Substring** directly.

## Complexity detail
- **Time Complexity**: $O(N + M)$ — Operation count bound.
- **Space Complexity**: $O(N + M)$ — Auxiliary memory allocation bound.
