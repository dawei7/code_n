## General
Implements the optimal algorithm for **Remove All Occurrences of a Substring** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(N + M)$ — Operation count bound.
- **Space Complexity**: $O(N + M)$ — Auxiliary memory allocation bound.
