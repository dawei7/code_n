## Description

Given the head of a linked list, reverse its nodes in consecutive groups of $k$ and return the modified list.

The positive integer $k$ does not exceed the list length. If fewer than $k$ nodes remain at the end, leave those nodes in their original order. Node values may not be altered; only node connections may change.
