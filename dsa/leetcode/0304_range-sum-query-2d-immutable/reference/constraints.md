## Constraints

- $m = \texttt{matrix.length}$
- $n = \texttt{matrix[i].length}$
- $1 \le m,n \le 200$
- $-10^4 \le \texttt{matrix[i][j]} \le 10^4$
- $0 \le \texttt{row1} \le \texttt{row2} < m$
- $0 \le \texttt{col1} \le \texttt{col2} < n$
- At most $10^4$ calls are made to `sumRegion`.
