## General
Executes a SQL query for **Students Report By Geography** using Common Table Expressions (CTEs), window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R \log R)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
