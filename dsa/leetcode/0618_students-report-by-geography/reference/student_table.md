## Student Table

| Column Name | Type |
|---|---|
| `name` | varchar |
| `continent` | varchar |

The table may contain duplicate rows. Each row gives a student's name and the continent from which that student came.
