## Constraints

- $1 \le \lvert\texttt{prices}\rvert \le 500$
- Every `prices[i]` represents a real number in $[0.0,1000.0]$ and contains exactly three decimal places.
- $0 \le \texttt{target} \le 10^6$
