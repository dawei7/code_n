## Description

Given an integer array `nums` of length $n$ and an integer `target`, choose values from three distinct indices so that their sum is as close as possible to `target`.

Return that three-value sum. Every input is guaranteed to have exactly one closest result.
