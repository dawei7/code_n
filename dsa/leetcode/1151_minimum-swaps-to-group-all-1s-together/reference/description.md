## Description

Given a binary array `data`, group every `1` in the array into one contiguous block located anywhere in the array. Return the minimum number of swaps required.

A swap may exchange values at two array positions; the positions need not be adjacent, as demonstrated by the source examples. The array may already meet the goal, and zero or one occurrence of `1` is already grouped.
