## General
Executes a SQL query for **Grand Slam Titles** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(C + K)$ — Operation count bound.
- **Space Complexity**: $O(C + K)$ — Auxiliary memory allocation bound.
