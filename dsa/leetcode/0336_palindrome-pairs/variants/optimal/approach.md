## General
Algorithm uses single-pass sequential scanning. Maintains hash map lookup (`dict`) for $O(1)$ average speed.

## Complexity detail
- **Time Complexity**: $O(S + P)$ — Operation count bound.
- **Space Complexity**: $O(S + P)$ — Auxiliary memory allocation bound.
