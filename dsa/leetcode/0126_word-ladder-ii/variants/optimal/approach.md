## General
Algorithm uses depth-first search recursion. Maintains hash map lookup (`dict`) for $O(1)$ average speed, double-ended queue (`deque`) for $O(1)$ window bounds.

## Complexity detail
- **Time Complexity**: $O(N + R)$ — Operation count bound.
- **Space Complexity**: $O(N + R)$ — Auxiliary memory allocation bound.
