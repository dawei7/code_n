## Constraints

- `rows == mat.length`
- `cols == mat[i].length`
- $1 \le \texttt{rows}, \texttt{cols} \le 100$
- Every `mat[i][j]` is either `0` or `1`.
- Every `mat[i]` is sorted in non-decreasing order.
