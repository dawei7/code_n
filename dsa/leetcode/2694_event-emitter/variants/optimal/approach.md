## General
Executes JavaScript logic for **Event Emitter** using ES6 Map/Set lookup structures, JavaScript array iteration methods. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(1 + k(a + 1))$ — Operation count bound.
- **Space Complexity**: $O(s + k)$ — Auxiliary memory allocation bound.
