## Sessions Table

| Column Name | Type |
|---|---|
| `session_id` | int |
| `duration` | int |

`session_id` contains unique values. `duration` records, in seconds, how long the user visited the application during that session.
