## Constraints

- $1 \le \texttt{s.length} \le 30$
- `s` contains lowercase English letters, digits, and square brackets `"[]"`.
- `s` is guaranteed to be valid.
- Every integer encoded in `s` lies in `[1,300]`.
