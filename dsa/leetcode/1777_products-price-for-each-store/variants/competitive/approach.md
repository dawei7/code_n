## General
Executes a SQL query for **Product's Price for Each Store** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R)$ — Operation count bound.
- **Space Complexity**: $O(P)$ — Auxiliary memory allocation bound.
