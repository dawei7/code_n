## Constraints

- $1 \le \texttt{word.length} \le 25$
- A word passed to `addWord` contains only lowercase English letters.
- A word passed to `search` contains only lowercase English letters and `'.'` characters.
- A `search` query contains at most two dots.
- At most $10^4$ total calls are made to `addWord` and `search`.
