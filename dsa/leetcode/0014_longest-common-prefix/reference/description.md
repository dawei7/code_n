## Description

Given an array of strings `strs`, return the longest prefix shared by every string. A prefix begins at the first character and remains contiguous.

If the strings have no common non-empty prefix, return the empty string `""`.
