## Description

Given a string `s`, return its longest palindromic substring. A substring must occupy one contiguous interval of `s`, and it is palindromic when it reads identically from left to right and from right to left.

If several palindromic substrings share the maximum length, any one of them is an acceptable result.
