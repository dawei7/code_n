## General
Executes JavaScript logic for **Memoize II** using ES6 Map/Set lookup structures. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(a)$ — Operation count bound.
- **Space Complexity**: $O(u * a)$ — Auxiliary memory allocation bound.
