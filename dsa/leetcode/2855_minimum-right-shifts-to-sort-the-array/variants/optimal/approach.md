## General
Implements the optimal algorithm for **Minimum Right Shifts to Sort the Array** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
