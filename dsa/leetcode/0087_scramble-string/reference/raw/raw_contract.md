## Function Contract

**Inputs**

- `s1`: The original lowercase string.
- `s2`: The equal-length candidate scrambled string.

**Return value**

Return `true` if some valid recursive split-and-optional-swap process transforms `s1` into `s2`; otherwise return `false`.
