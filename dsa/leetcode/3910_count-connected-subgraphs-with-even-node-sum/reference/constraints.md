## Constraints

- $1 \le n = \lvert\texttt{nums}\rvert \le 13$.
- Every `nums[i]` is either `0` or `1`.
- $0 \le \lvert\texttt{edges}\rvert \le n(n-1)/2$.
- Every edge has the form `[u, v]`.
- For each edge, $0 \le u < v < n$.
- All edges are distinct.
