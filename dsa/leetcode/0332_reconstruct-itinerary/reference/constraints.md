## Constraints

- $1 \le \texttt{tickets.length} \le 300$
- `tickets[i].length == 2`
- `from_i.length == 3`
- `to_i.length == 3`
- `from_i` and `to_i` contain only uppercase English letters.
- `from_i != to_i`
