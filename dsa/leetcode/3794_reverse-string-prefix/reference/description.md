## Description

You are given a string `s` and an integer `k`.

Reverse the order of exactly the first `k` characters. The character originally at position `k` becomes the first character, the original first character becomes the last character of that prefix, and every character after the prefix remains in its original position and order.

Return the string produced by joining the reversed prefix with the unchanged suffix. The operation changes neither the string's length nor its characters.
