## Description

Given two strings `s` and `t`, return the number of distinct subsequences of `s` that equal `t`. A subsequence retains the chosen characters' original relative order while allowing unchosen characters to be removed.

Every generated test case has an answer that fits in a signed 32-bit integer.
