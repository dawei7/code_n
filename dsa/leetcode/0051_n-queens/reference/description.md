## Description

The n-queens puzzle asks you to place $n$ queens on an $n \times n$ chessboard so that no pair of queens attacks one another.

Given `n`, return every distinct solution. The solutions may appear in any order. Represent each board as $n$ strings: `Q` marks a queen and `.` marks an empty square.
