## Description

Given the root of a binary tree, find the subtree with the greatest number of nodes that is also a Binary Search Tree (BST). Return that subtree's size.

A tree is a BST when every node satisfies both of these properties:

- Every value in its left subtree is strictly less than the node's value.
- Every value in its right subtree is strictly greater than the node's value.
