## Function Contract

**Inputs**

- `board`: A $9 \times 9$ Sudoku grid containing fixed digit clues and `.` characters for empty cells.

**Return value**

Return `None`; modify `board` in place to contain the completed grid while preserving every original clue.
