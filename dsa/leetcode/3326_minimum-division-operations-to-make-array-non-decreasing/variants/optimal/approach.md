## General
Given an integer array `nums`, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(U log log U + n)$ — Operation count bound.
- **Space Complexity**: $O(U + n)$ — Auxiliary memory allocation bound.
