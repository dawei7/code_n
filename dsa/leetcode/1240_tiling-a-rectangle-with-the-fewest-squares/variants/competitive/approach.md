## General
Uses depth-first search / backtracking recursion. Maintains hash map (`dict`) for $O(1)$ average lookup. Applies bitwise operations (`&`, `|`, `^`, `<<`, `>>`).

## Complexity detail
- **Time Complexity**: $O\left(wh(h+1)^w\right)$ — Operation count bound.
- **Space Complexity**: $O\left((h+1)^w\right)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
