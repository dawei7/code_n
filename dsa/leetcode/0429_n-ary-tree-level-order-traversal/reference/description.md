## Description

Given an N-ary tree, return the level-order traversal of its node values. Group all values at the same depth
together, beginning with the root level and proceeding downward while preserving the nodes' left-to-right order.

In the source examples, an N-ary tree is serialized in level order and `null` separates one parent's child group
from the next.
