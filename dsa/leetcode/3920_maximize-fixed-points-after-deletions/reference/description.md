## Description

An index `i` of an integer array `nums` is a fixed point when the value stored there equals the index: `nums[i] == i`.

You may delete any collection of elements, including none or all of them. Every deletion closes the gap by shifting later retained elements to the left, and the resulting array is indexed again from `0`.

Choose the deletions that produce as many fixed points as possible, and return that maximum count.
