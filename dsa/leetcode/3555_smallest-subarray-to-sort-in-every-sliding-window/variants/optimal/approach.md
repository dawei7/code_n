## General
Algorithm uses two-pointer sliding window iteration.

## Complexity detail
- **Time Complexity**: $O((n-k+1)k)$ — Operation count bound.
- **Space Complexity**: $O(k)$ — Auxiliary memory allocation bound.
