## General
Executes a SQL query for **The Most Recent Three Orders** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O(m\log m + c)$ — Operation count bound.
- **Space Complexity**: $O(m)$ — Auxiliary memory allocation bound.
