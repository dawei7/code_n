## Constraints

- `Customers` and `Orders` tables contain up to $10^5$ rows.
- Each customer has at most one order per date.
- Output columns must be `customer_name`, `customer_id`, `order_id`, and `order_date`.
