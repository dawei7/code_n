## Constraints

- $1 \le \texttt{nums.length} \le 1000$
- $1 \le \texttt{nums[i]} \le 1000$
- Every call to `next` or `peek` is valid.
- At most `1000` total calls are made to `next`, `hasNext`, and `peek`.
