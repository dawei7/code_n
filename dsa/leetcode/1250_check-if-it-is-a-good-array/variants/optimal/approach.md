## General
Implements the optimal algorithm for **Check If It Is a Good Array** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n \log M)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
