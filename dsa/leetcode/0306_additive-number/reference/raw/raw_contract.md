## Function Contract

**Inputs**

- `num`: The complete decimal-digit string to partition into consecutive terms.

**Return value**

Return `True` when `num` admits a valid additive partition that consumes every digit; otherwise return `False`.
