## General
Algorithm uses single-pass sequential scanning.

## Complexity detail
- **Time Complexity**: $O(amount \cdot |coins|)$ — Operation count bound.
- **Space Complexity**: $O(amount)$ — Auxiliary memory allocation bound.
