## Hint

**Hint 1:** Suppose a range-minimum-query structure, such as a sparse table, can report the minimum of any array interval in $O(1)$ time. Consider how that would let you test whether a proposed subarray is valid.

**Hint 2:** For each starting index, combine range-minimum queries with binary search to locate the furthest possible ending position.
