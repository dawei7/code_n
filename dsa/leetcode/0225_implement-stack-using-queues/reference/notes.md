## Notes

- Use only standard queue operations: push to the back, inspect or pop from the front, obtain the size, and test whether the queue is empty.
- If the language has no native queue, a list or deque may simulate one, provided only those queue operations are used.
