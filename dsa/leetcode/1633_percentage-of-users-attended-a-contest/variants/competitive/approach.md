## General
Executes a SQL query for **Percentage of Users Attended a Contest** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(u+r+c\log c)$ — Operation count bound.
- **Space Complexity**: $O(c)$ — Auxiliary memory allocation bound.
