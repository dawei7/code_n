## General
Given two **positive** integers `num` and `sum`, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(num)$ — Operation count bound.
- **Space Complexity**: $O(num)$ — Auxiliary memory allocation bound.
