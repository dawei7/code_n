## Description

Given an integer array `nums` and an integer `target`, identify two array positions whose values sum to `target`. Return the indices of those positions, not the values stored there.

Every input has exactly one valid answer. The two positions must be distinct, so one array element cannot be selected twice even when twice its value would equal `target`.

The two result indices may be returned in either order.
