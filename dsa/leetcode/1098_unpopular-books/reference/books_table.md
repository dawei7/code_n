## Books Table

| Column Name | Type |
|---|---|
| `book_id` | int |
| `name` | varchar |
| `available_from` | date |

`book_id` is the primary key, so each value identifies one book. The row also gives that book's name and the date from which it became available.
