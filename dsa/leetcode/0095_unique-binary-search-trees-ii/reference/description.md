## Description

Given `n`, return every structurally distinct binary search tree containing exactly `n` nodes whose unique values are the integers from `1` through `n`.

The trees may be returned in any order.
