## Description

The `Candidates` table stores each candidate's identifier, name, years of experience, and interview identifier. The `Rounds` table stores the score for each round belonging to an interview.

Report the identifier of every candidate who has at least two years of experience and whose interview's round scores sum to strictly more than `15`. Return one row per qualifying candidate; the result rows may appear in any order.
