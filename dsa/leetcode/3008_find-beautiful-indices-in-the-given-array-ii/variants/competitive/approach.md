## General
Algorithm uses single-pass sequential scanning. Edge cases: handles empty/null inputs via early return guards.

## Complexity detail
- **Time Complexity**: $O(N+A+B)$ — Operation count bound.
- **Space Complexity**: $O(N+A+B)$ — Auxiliary memory allocation bound.
