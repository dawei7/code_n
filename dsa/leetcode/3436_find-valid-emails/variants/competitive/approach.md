## General
Given Table: `Users`, the database query executes a relational database query for **Find Valid Emails**.

## Complexity detail
- **Time Complexity**: $O(S + r log r)$ — Operation count bound.
- **Space Complexity**: $O(r)$ — Auxiliary memory allocation bound.
