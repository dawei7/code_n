## Description

Find the size of the team to which each employee belongs. An employee's team size is the total number of rows in `Employee` having the same `team_id` as that employee.

Return one row for every employee, containing the employee's identifier and the corresponding team size. The rows may appear in any order.
