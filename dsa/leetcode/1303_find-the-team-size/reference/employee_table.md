## Employee Table

| Column Name | Type |
|---|---|
| `employee_id` | `int` |
| `team_id` | `int` |

`employee_id` is the primary key, so each employee identifier appears once. Every row associates one employee with that employee's team.
