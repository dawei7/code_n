## General
Given Table: `Employee`, the database query executes a relational database query for **Second Highest Salary**.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
