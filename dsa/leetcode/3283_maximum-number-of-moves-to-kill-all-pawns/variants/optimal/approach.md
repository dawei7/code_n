## General
Uses depth-first search / backtracking recursion. Maintains double-ended queue (`deque`) for $O(1)$ operations. Applies bitwise operations (`&`, `|`, `^`, `<<`, `>>`).

## Complexity detail
- **Time Complexity**: $O(p B + p^2 2^p)$ — Operation count bound.
- **Space Complexity**: $O(B + p 2^p)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
