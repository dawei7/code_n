## Queries Table

| Column Name | Type |
|---|---|
| `id` | int |
| `year` | int |

The pair `(id, year)` is the table's composite primary key. Each row requests the value for one inventory ID and year.
