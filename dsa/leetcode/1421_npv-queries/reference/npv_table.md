## Npv Table

| Column Name | Type |
|---|---|
| `id` | int |
| `year` | int |
| `npv` | int |

The pair `(id, year)` is the table's composite primary key. Each row associates one inventory ID and year with its net present value.
