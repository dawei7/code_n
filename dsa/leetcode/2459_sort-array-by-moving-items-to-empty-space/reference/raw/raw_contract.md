## Function Contract

**Inputs**

- `nums`: A permutation of all integers from $0$ to $n - 1$ ($2 \le n \le 10^5$).

**Return value**

Return an integer representing the minimum number of operations into the empty space needed to obtain either `[0, 1, ..., n - 1]` or `[1, 2, ..., n - 1, 0]`.
