## Constraints

- $1 \le \texttt{numCourses} \le 2000$
- $0 \le \texttt{prerequisites.length} \le 5000$
- `prerequisites[i].length == 2`
- $0 \le a_i, b_i < \texttt{numCourses}$
- Every pair in `prerequisites` is unique.
