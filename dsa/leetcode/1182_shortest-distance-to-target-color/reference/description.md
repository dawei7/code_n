## Description

The array `colors` represents a fixed row whose entries use exactly the three possible color values `1`, `2`, and `3`.

Each query supplies an array position `i` and a target color `c`. For that query, find the shortest distance from `i` to any position containing `c`. If the target color does not occur anywhere in `colors`, the corresponding result is `-1`.
