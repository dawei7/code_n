## Hint

**Hint 1:** A greedy search performed separately for every query is too slow at the stated limits.

**Hint 2:** Consider answering the queries offline after preprocessing the fixed `colors` array.

**Hint 3:** For every position, compute target-color distances from both its left and its right.
