## General
Executes a SQL query for **Year on Year Growth Rate** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(t log t)$ — Operation count bound.
- **Space Complexity**: $O(t)$ — Auxiliary memory allocation bound.
