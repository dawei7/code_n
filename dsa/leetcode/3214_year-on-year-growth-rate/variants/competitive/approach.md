## General
Executes a SQL query for **Year on Year Growth Rate** using Common Table Expressions (CTEs), window functions, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O(t log t)$ — Operation count bound.
- **Space Complexity**: $O(t)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
