## Constraints

- $1 \le \texttt{nestedList.length} \le 50$
- Every integer value in the nested list lies in $[-100,100]$.
- The maximum depth of any integer is at most `50`.
- No list in the structure is empty.
