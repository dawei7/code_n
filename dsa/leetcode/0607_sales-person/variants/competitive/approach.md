## General
Given Table: `SalesPerson`, the database query executes a relational pipeline using relational `JOIN` operations to correlate matching records across tables.

## Complexity detail
- **Time Complexity**: $O((S + C + O) \log(S + C + O))$ — Operation count bound.
- **Space Complexity**: $O(S + C + O)$ — Auxiliary memory allocation bound.
