## General
Executes a SQL query for **Generate the Invoice** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O((P+R)\log(P+R))$ — Operation count bound.
- **Space Complexity**: $O(P+R)$ — Auxiliary memory allocation bound.
