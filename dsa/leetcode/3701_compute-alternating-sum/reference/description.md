## Description

For the integer array `nums`, form its **alternating sum** by adding the elements at even indices and subtracting the elements at odd indices. In index order, the expression begins as

`nums[0] - nums[1] + nums[2] - nums[3] + ...`.

Return the resulting integer.
