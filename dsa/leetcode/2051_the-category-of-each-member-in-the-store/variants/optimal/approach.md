## General
Given Table: `Members`, the database query executes a relational pipeline using relational `JOIN` operations to correlate matching records across tables, `GROUP BY` aggregations to summarize record groups.

## Complexity detail
- **Time Complexity**: $O(M+V+P)$ — Operation count bound.
- **Space Complexity**: $O(M+V+P)$ — Auxiliary memory allocation bound.
