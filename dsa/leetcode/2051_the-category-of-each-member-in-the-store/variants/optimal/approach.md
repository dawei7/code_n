## General
Executes a SQL query for **The Category of Each Member in the Store** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(M+V+P)$ — Operation count bound.
- **Space Complexity**: $O(M+V+P)$ — Auxiliary memory allocation bound.
