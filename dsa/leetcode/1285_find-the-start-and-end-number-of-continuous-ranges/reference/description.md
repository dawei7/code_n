## Description

The identifiers in `Logs` form one or more continuous integer ranges. Within a range, every integer from its start through its end appears in the table; a missing integer separates that range from the next one.

Find the start and end identifier of every maximal continuous range in `Logs`. Return the ranges ordered by `start_id`.
