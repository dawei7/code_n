## Hints

1. Convert the represented decimal integer to hexadecimal first.
2. Substitute `O` for every `0` and `I` for every `1` in that hexadecimal representation.
3. Determine whether any numerical digit remains after those substitutions.
