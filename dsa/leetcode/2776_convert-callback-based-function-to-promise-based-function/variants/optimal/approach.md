## General
Executes JavaScript logic for **Convert Callback Based Function to Promise Based Function** using idiomatic control flow. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(a)$ — Operation count bound.
- **Space Complexity**: $O(a)$ — Auxiliary memory allocation bound.
