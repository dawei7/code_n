## Description

A **distinct string** is a string that is present only **once** in an array.

Given an array of strings `arr`, and an integer `k`, return *the *`k^th`***distinct string</strong> present in </em>`arr`. If there are **fewer** than `k` distinct strings, return <em>an **empty string ***`""`.

Note that the strings are considered in the <strong>order in which they appear** in the array.
