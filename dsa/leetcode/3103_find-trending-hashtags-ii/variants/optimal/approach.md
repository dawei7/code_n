## General
Given Table: `Tweets`, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(S + h + g log g)$ — Operation count bound.
- **Space Complexity**: $O(S + h + g)$ — Auxiliary memory allocation bound.
