## Constraints

- $2 \leq \lvert\texttt{lists}\rvert \leq 12$
- $1 \leq \lvert\texttt{lists[i]}\rvert \leq 500$
- $-10^9 \leq \texttt{lists[i][j]} \leq 10^9$
- Every `lists[i]` is sorted in non-decreasing order.
- The sum of all `lists[i].length` values is at most $2000$.
