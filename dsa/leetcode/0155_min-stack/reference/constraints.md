## Constraints

- $-2^{31} \le \texttt{value} \le 2^{31} - 1$
- `pop`, `top`, and `getMin` are called only when the stack is non-empty.
- At most $3 \times 10^4$ total calls are made to `push`, `pop`, `top`, and `getMin`.
