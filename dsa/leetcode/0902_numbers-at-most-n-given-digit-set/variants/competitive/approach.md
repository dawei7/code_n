## General
Given an array of `digits` which is sorted in **non-decreasing** order. You can write numbers using each $\text{digits}[i]$ as many times as we want. For example, if $digits = ['1','3','5']$, we may write numbers such as `'..., the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(d\log k)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
