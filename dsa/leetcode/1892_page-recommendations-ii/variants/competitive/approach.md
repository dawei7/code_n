## General
Executes a SQL query for **Page Recommendations II** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((F+L+C)\log C)$ — Operation count bound.
- **Space Complexity**: $O(F+L+C)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
