## Description

Given a binary-tree root, return the length of its longest consecutive sequence path.

Along a consecutive sequence path, each next node's value is exactly one greater than the preceding node's value. The path may begin at any node, but it can travel only from a parent to a child; it cannot move upward from a node to its parent.
