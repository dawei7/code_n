## Hints

1. Traverse the tree with depth-first search.
2. Determine the value sum of every node's subtree.
3. Traverse again from the root, stopping whenever a node whose subtree sum is zero is reached.
