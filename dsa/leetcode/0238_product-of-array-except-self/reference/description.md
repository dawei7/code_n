## Description

Given an integer array `nums`, return an array `answer` in which `answer[i]` equals the product of every element of `nums` except `nums[i]`.

The product of every prefix or suffix of `nums` is guaranteed to fit in a signed 32-bit integer.

The algorithm must run in $O(n)$ time and must not use division.
