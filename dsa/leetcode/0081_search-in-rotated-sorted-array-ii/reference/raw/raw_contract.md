## Function Contract

**Inputs**

- `nums`: A rotated non-decreasing integer array that may contain duplicates.
- `target`: The integer to find.

**Return value**

Return `true` if `target` appears in `nums`; otherwise return `false`.
