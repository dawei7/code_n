## General
Executes a SQL query for **Ad-Free Sessions** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O((P + A)\log(A + 1))$ — Operation count bound.
- **Space Complexity**: $O(P + A)$ — Auxiliary memory allocation bound.
