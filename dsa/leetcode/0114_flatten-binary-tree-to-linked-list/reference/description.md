## Description

Given the `root` of a binary tree, flatten the tree into a linked-list-shaped sequence subject to both of these rules:

- Reuse the existing `TreeNode` representation. Every `right` pointer leads to the next node, and every `left` pointer is `null`.
- Arrange the nodes in the same order as a preorder traversal of the original tree.
