## Constraints

- `Warehouse` and `Products` tables contain up to $10^5$ rows.
- Dimensions `Width`, `Length`, and `Height` are positive integers.
- Output column names must be `warehouse_name` and `volume`.
