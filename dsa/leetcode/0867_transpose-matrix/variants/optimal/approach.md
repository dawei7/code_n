## General
Implements the optimal algorithm for **Transpose Matrix** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(mn)$ — Operation count bound.
- **Space Complexity**: $O(mn)$ — Auxiliary memory allocation bound.
