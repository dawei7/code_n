## General
Implements the optimal algorithm for **Median of Two Sorted Arrays** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(\log(\min(m, n)))$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
