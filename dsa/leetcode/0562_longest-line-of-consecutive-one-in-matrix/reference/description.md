## Description

Given an $m \times n$ binary matrix `mat`, return the length of its longest line of consecutive `1` cells.

A valid line uses adjacent cells in exactly one of four fixed directions: horizontal, vertical, diagonal from upper
left to lower right, or anti-diagonal from upper right to lower left. A `0` ends a line. Gaps, turns, and combinations
of different directions cannot be counted as one line.
