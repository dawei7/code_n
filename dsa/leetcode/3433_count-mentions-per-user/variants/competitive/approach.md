## General
Algorithm uses single-pass sequential scanning.

## Complexity detail
- **Time Complexity**: $O(E log E + E log U + M + U)$ — Operation count bound.
- **Space Complexity**: $O(E + U)$ — Auxiliary memory allocation bound.
