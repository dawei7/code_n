## Description

You are given two strings `s` and `target`. They have the same length `n` and contain only lowercase English letters.

Return the lexicographically smallest string that is both a palindromic permutation of `s` and strictly greater than `target`. If no string satisfies both conditions, return an empty string.
