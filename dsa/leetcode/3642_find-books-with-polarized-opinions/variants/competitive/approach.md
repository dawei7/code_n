## General
Executes a SQL query for **Find Books with Polarized Opinions** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R + B log B)$ — Operation count bound.
- **Space Complexity**: $O(B)$ — Auxiliary memory allocation bound.
