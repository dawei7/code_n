## Description

You receive the three positive integer side lengths in an array `sides`. First decide whether those lengths can form a triangle with positive area.

When they cannot, return an empty array. When they can, return the triangle's three internal angles measured in degrees and arranged in non-decreasing order.

Each returned angle is accepted when it differs from the exact value by at most $10^{-5}$.
