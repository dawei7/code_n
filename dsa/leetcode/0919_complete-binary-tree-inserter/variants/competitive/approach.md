## General
Algorithm uses single-pass sequential scanning. Maintains tree node traversal (`val`, `left`, `right`). Edge cases: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(n + q)$ — Operation count bound.
- **Space Complexity**: $O(m)$ — Auxiliary memory allocation bound.
