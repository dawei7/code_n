## General
Given two positive integers `n` and `target`, the algorithm solves **Minimum Addition to Make Integer Beautiful** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(log n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
