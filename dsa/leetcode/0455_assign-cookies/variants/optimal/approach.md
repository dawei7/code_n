## General
Given Assume you are an awesome parent and want to give your children some cookies. But, you should give each child at most one cookie, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(g \log g + s \log s)$ — Operation count bound.
- **Space Complexity**: $O(g + s)$ — Auxiliary memory allocation bound.
