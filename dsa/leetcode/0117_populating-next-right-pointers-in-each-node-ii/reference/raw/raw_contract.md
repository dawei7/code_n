## Function Contract

**Inputs**

- `root`: The root of an arbitrary binary tree, or `null` for an empty tree.

**Return value**

Return the original root after populating every horizontal `next` link. The app serializes each linked level and uses `#` to mark its end.
