## General
Uses depth-first search / backtracking recursion. Maintains hash set (`set`) for $O(1)$ existence checks, double-ended queue (`deque`) for $O(1)$ operations.

## Complexity detail
- **Time Complexity**: $O(V)$ — Operation count bound.
- **Space Complexity**: $O(V)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
