## Constraints

- $1 \le \lvert\texttt{words}\rvert \le 300$
- $1 \le \lvert\texttt{words[i]}\rvert \le 20$
- Every `words[i]` contains only English letters and symbols.
- $1 \le \texttt{maxWidth} \le 100$
- `words[i].length <= maxWidth`
