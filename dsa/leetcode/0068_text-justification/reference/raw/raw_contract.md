## Function Contract

**Inputs**

- `words`: The non-empty words to lay out in their given order.
- `maxWidth`: The exact character width required for every output line.

**Return value**

Return the greedily packed and justified lines. Every returned string has length `maxWidth`.
