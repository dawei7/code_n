## Follow-up

- If the supplied arrays are already sorted, how would you optimize the method?
- If `nums1` is much smaller than `nums2`, which method is preferable?
- If the elements of `nums2` reside on disk and memory cannot hold all of them simultaneously, how would you adapt the solution?
