## General
Algorithm uses depth-first search recursion. Maintains tree node traversal (`val`, `left`, `right`). Edge cases: handles empty/null inputs via early return guards, uses infinity sentinels for extreme boundary comparisons.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(h)$ — Auxiliary memory allocation bound.
