## General
Executes a SQL query for **List the Products Ordered in a Period** using relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O(p+o+k\log k)$ — Operation count bound.
- **Space Complexity**: $O(p+k)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
