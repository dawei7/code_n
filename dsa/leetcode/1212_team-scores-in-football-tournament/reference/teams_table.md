## Teams Table

The `Teams` table has the following schema:

| Column Name | Type |
|---|---|
| `team_id` | int |
| `team_name` | varchar |

`team_id` contains unique values. Each row represents one football team.
