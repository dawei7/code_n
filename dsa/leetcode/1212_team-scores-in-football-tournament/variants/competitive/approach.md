## General
Executes a SQL query for **Team Scores in Football Tournament** using relational JOINs, GROUP BY aggregations. Edge cases: replaces NULL values using `COALESCE` guard.

## Complexity detail
- **Time Complexity**: $O(t+m)$ — Operation count bound.
- **Space Complexity**: $O(t+m)$ — Auxiliary memory allocation bound.
