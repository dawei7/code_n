## General
Given an `n x n` binary `grid`, in one step you can choose two **adjacent rows** of the grid and swap them, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(n^2)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
