## General
Executes a SQL query for **Bank Account Summary II** using relational JOINs, GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O((U+T)\log(U+T))$ — Operation count bound.
- **Space Complexity**: $O(U+T)$ — Auxiliary memory allocation bound.
