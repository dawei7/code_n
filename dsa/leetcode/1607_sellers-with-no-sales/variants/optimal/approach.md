## General
Executes a SQL query for **Sellers With No Sales** using relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((s+r)\log(s+r))$ — Operation count bound.
- **Space Complexity**: $O(s+r)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
