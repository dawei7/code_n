## Constraints

- $n = \lvert\texttt{s}\rvert = \lvert\texttt{t}\rvert$
- $1 \leq n \leq 10^5$
- $1 \leq \texttt{flipCost}, \texttt{swapCost}, \texttt{crossCost} \leq 10^9$
- `s` and `t` contain only the characters `'0'` and `'1'`.
