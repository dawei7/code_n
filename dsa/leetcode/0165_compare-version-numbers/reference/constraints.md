## Constraints

- $1 \le \texttt{version1.length}, \texttt{version2.length} \le 500$
- `version1` and `version2` contain only digits and the dot character `.`.
- Both strings are valid version numbers.
- Every supplied revision can be stored in a signed 32-bit integer.
