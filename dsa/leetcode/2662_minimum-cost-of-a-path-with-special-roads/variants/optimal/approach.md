## General
Uses single-pass sequential iteration. Maintains hash set (`set`) for $O(1)$ existence checks, priority queue (`heapq`) for dynamic minimum/maximum tracking.

## Complexity detail
- **Time Complexity**: $O(r^2 log r)$ — Operation count bound.
- **Space Complexity**: $O(r^2)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
