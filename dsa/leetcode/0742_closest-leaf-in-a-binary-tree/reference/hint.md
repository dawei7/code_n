## Hint

One option is to convert the tree into a general graph and run breadth-first search. Alternatively, determine the nearest leaf associated with every node along the path from the root to the target.
