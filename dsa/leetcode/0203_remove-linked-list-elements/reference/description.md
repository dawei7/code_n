## Description

Given the `head` of a linked list and an integer `val`, remove every node for which `Node.val == val`.

Return the head of the resulting list after all matching nodes have been removed.
