## User Activity Table

| Column Name | Type |
|---|---|
| `username` | `varchar` |
| `activity` | `varchar` |
| `startDate` | `Date` |
| `endDate` | `Date` |

The table may contain duplicate rows. Each row states that `username` performed the named activity from `startDate` through `endDate`.
