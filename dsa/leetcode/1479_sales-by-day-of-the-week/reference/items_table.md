## Items Table

`Items` supplies the name and category for every item identifier.

| Column Name | Type |
|---|---|
| `item_id` | `varchar` |
| `item_name` | `varchar` |
| `item_category` | `varchar` |

The `item_id` column is the table's primary key. The other two columns contain
the item's name and category.
