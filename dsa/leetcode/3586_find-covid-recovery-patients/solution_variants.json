{
  "schema_version": 1,
  "challenge_id": "lc_3586",
  "default_variant": "optimal",
  "variants": [
    {
      "id": "optimal",
      "label": "Optimal",
      "kind": "optimal",
      "directory": "variants/optimal",
      "summary": "Aggregates each patient's earliest positive once, then joins and aggregates the earliest strictly later negative before ordering recovered patients.",
      "time_complexity": "O(T log T + P log P)",
      "space_complexity": "O(T + P)"
    }
  ]
}
