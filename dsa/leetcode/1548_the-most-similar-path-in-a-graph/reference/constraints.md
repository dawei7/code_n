## Constraints

- $2 \le n \le 100$.
- $1 \le \text{roads.length} \le \frac{n(n-1)}{2}$.
- $1 \le m \le 100$ where $m = \text{targetPath.length}$.
- The graph is connected and contains no self-loops or duplicate edges.
- `names[i]` and `targetPath[j]` consist of uppercase English letters.
