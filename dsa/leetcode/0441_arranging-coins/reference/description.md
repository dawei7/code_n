## Description

You have `n` coins with which to build a staircase. If the staircase has `k` rows, row `i` requires exactly `i`
coins. The last row is allowed to be incomplete.

Return the number of complete rows that can be built.
