## Product Table

| Column Name | Type |
|---|---|
| `product_id` | int |
| `product_name` | varchar |

`product_id` is the primary key, so its values are unique. Each row associates one product identifier with that product's name.
