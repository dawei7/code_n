## Function Contract

**Inputs**

- `arr`: a non-empty array of distinct integers in ascending order.

Let $N = \lvert\texttt{arr}\rvert$. Indices are zero-based, so the positions range from $0$ through $N-1$.

**Return value**

- The smallest index `i` such that `arr[i] == i`, or `-1` when no such index exists.
