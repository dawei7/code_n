## General
Given a list of non-negative integers `nums`, arrange them such that they form the largest number and return it, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(nk \log n)$ — Operation count bound.
- **Space Complexity**: $O(nk)$ — Auxiliary memory allocation bound.
