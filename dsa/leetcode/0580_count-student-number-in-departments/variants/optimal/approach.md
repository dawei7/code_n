## General
Given Table: `Student`, the database query executes a relational pipeline using relational `JOIN` operations to correlate matching records across tables, `GROUP BY` aggregations to summarize record groups.

## Complexity detail
- **Time Complexity**: $O((D + S) \log D)$ — Operation count bound.
- **Space Complexity**: $O(D + S)$ — Auxiliary memory allocation bound.
