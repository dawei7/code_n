## General
Executes a SQL query for **Count Student Number in Departments** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O((D + S) \log D)$ — Operation count bound.
- **Space Complexity**: $O(D + S)$ — Auxiliary memory allocation bound.
