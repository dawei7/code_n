## Department Table

| Column Name | Type |
|---|---|
| `dept_id` | int |
| `dept_name` | varchar |

`dept_id` is the primary key, so its values are unique. Each row associates a department identifier with its name.
