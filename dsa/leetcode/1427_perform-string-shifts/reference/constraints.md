## Constraints

- $1 \le \lvert\texttt{s}\rvert \le 100$
- `s` contains only lowercase English letters.
- $1 \le \lvert\texttt{shift}\rvert \le 100$
- Every row has exactly two entries: $\lvert\texttt{shift[i]}\rvert = 2$.
- `direction_i` is either `0` or `1`.
- $0 \le \texttt{amount_i} \le 100$
