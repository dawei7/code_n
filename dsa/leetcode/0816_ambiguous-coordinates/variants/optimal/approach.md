## General
Algorithm uses two-pointer sliding window iteration.

## Complexity detail
- **Time Complexity**: $O(n^3 + nK)$ — Operation count bound.
- **Space Complexity**: $O(n^2 + nK)$ — Auxiliary memory allocation bound.
