## Constraints

- $1 \le \lvert\texttt{requests}\rvert \le 10^5$
- Every record has the form `requests[i] = [user_i, time_i]`.
- $1 \le \texttt{k} \le \lvert\texttt{requests}\rvert$
- $1 \le \texttt{user_i},\texttt{time_i},\texttt{window} \le 10^5$
