## Function Contract

**Inputs**

- `num`: A nonempty string containing only decimal digits.
- `target`: The integer value each returned expression must produce.

**Return value**

Return all expressions formed from the digits of `num` and the operators `+`, `-`, and `*` that evaluate to `target` without using leading-zero operands.
