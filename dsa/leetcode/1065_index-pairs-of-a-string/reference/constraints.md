## Constraints

- $1 \le \lvert\texttt{text}\rvert \le 100$
- $1 \le \lvert\texttt{words}\rvert \le 20$
- $1 \le \lvert\texttt{words[i]}\rvert \le 50$
- `text` and every `words[i]` consist only of lowercase English letters.
- All strings in `words` are distinct.
