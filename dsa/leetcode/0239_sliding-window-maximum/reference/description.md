## Description

An integer array `nums` is viewed through a sliding window of size `k`. The first window covers the leftmost `k` values, and each subsequent window moves exactly one position to the right until it reaches the array's end.

Return the maximum value visible at every window position, in movement order.
