## General
Given an integer `n`, return *`true` if it is a power of three. Otherwise, return `false`*, the algorithm solves **Power of Three** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
