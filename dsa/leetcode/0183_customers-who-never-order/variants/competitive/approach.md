## General
Executes a SQL query for **Customers Who Never Order** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O(c + o)$ — Operation count bound.
- **Space Complexity**: $O(c + o)$ — Auxiliary memory allocation bound.
