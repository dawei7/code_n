## Constraints

- $-10^7 \leq x \leq 10^7$.
- At most $10^5$ calls are made to `push`, `pop`, `top`, `peekMax`, and `popMax`.
- The stack contains at least one element whenever `pop`, `top`, `peekMax`, or `popMax` is called.
