## Constraints

- $1 \le \texttt{expression.length} \le 20$
- `expression` contains digits and the operators `'+'`, `'-'`, and `'*'`.
- Every integer value in `expression` lies in `[0,99]`.
- Input integers do not have a leading `'+'` or `'-'` sign.
