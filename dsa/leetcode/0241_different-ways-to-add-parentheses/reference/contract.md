## Function Contract

**Inputs**

- `expression`: An operator-separated expression containing unsigned integers and `+`, `-`, and `*`.

**Return value**

Return one integer for each possible full grouping, in any order and with duplicate results preserved.
