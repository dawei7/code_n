## General
Given an array `points` of size `n` and an integer `m`. There is another array `gameScore` of size `n`, where $\text{gameScore}[i]$ represents the score achieved at the $$i^{\text{th}}$$ game. Initially, $\text{gameScore}[i..., the algorithm executes binary search over the search space to achieve logarithmic reduction. It utilizes a hash map (`dict`) to store element values and their indices/frequencies for $O(1)$ fast access.

## Complexity detail
- **Time Complexity**: $O(n log(m p + 1))$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
