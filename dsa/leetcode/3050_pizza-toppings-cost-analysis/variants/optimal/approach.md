## General
Executes a SQL query for **Pizza Toppings Cost Analysis** using Common Table Expressions (CTEs), relational JOINs, window ranking functions.

## Complexity detail
- **Time Complexity**: $O(n^3 log n)$ — Operation count bound.
- **Space Complexity**: $O(n^3)$ — Auxiliary memory allocation bound.
