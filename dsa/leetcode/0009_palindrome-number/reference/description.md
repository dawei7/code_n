## Description

Given an integer `x`, determine whether its usual decimal representation is a palindrome. A palindromic value reads identically from left to right and from right to left.

Return `true` when the representation is palindromic and `false` otherwise.
