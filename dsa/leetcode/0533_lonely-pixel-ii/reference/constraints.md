## Constraints

- $m = \lvert \texttt{picture} \rvert$
- $n = \lvert \texttt{picture[i]} \rvert$
- $1 \le m, n \le 200$
- Every `picture[i][j]` is `"W"` or `"B"`.
- $1 \le \texttt{target} \le \min(m, n)$
