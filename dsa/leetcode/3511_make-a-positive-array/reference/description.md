## Description

You are given an integer array `nums`.

In one operation, you can choose an index `i` and increase `nums[i]` by `1`.

Return the **minimum** number of operations required to make all prefix sums of `nums` strictly positive.
