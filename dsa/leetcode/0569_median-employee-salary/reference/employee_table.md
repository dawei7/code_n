## Employee Table

| Column Name | Type |
|---|---|
| `id` | int |
| `company` | varchar |
| `salary` | int |

`id` is the primary key, so every employee row has a distinct identifier. Each row associates one employee with a company and a salary.
