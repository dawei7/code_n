## Method read

Implement `read(buf, n)` using only `read4`. Each call copies at most `n` next characters into `buf` and returns the number copied. Because `read` can be invoked repeatedly, any characters fetched by `read4` but not consumed by the current request must remain available to later calls.
