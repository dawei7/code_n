## Definition of read

- Parameters: `char[] buf, int n`
- Return type: `int`

`buf` is a destination, not an input source. The same destination buffer is used for the repeated calls in one test case.
