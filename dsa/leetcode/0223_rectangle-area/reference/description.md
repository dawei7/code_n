## Description

Two **rectilinear** rectangles lie in a two-dimensional coordinate plane. Return the total area covered by their union.

Rectangle A has bottom-left corner `(ax1,ay1)` and top-right corner `(ax2,ay2)`. Rectangle B is defined in the same way by `(bx1,by1)` and `(bx2,by2)`.
