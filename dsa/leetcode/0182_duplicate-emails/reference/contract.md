## Function Contract

**Inputs**

`Person(id, email)` contains non-null, lowercase email addresses.

**Return value**

Return one row for each duplicated address under the column `Email`, in any order.
