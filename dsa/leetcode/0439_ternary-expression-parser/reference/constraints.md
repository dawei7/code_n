## Constraints

- $5 \le n \le 10^4$, where $n$ is `expression.length`.
- `expression` contains only digits, `T`, `F`, `?`, and `:`.
- `expression` is guaranteed to be a valid ternary expression, and every number in it is a single digit.
