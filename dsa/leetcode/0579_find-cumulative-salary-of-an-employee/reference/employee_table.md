## Employee Table

| Column Name | Type |
|---|---|
| `id` | int |
| `month` | int |
| `salary` | int |

The pair `(id, month)` is the primary key, so an employee has at most one salary row for any month. Each row records that employee's salary in one month of 2020.
