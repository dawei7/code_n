## Constraints

- $1 \le n = \lvert\texttt{nums}\rvert \le 8$
- $0 \le \texttt{nums[i]} \le n-1$
- $1 \le \lvert\texttt{pre}\rvert \le n$
- $1 \le \texttt{pre[i]} \le n$
- `nums` is a permutation of the integers from $0$ through $n-1$.
- All values in `pre` are unique.
