## General
Executes a SQL query for **Friday Purchase III ** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations. Edge cases: replaces NULL values using `COALESCE` guard.

## Complexity detail
- **Time Complexity**: $O(u + p)$ — Operation count bound.
- **Space Complexity**: $O(u + p)$ — Auxiliary memory allocation bound.
