## General
Given a positive integer `n` representing `n` cities numbered from `1` to `n`. You are also given a **2D** array `roads` where $\text{roads}[i] = [a_{i}, b_{i}, \text{distance}_{i}]$ indicates that there is a **bidirectiona..., the algorithm executes breadth-first search (BFS) level-order traversal using a queue.

## Complexity detail
- **Time Complexity**: $O(n+m)$ — Operation count bound.
- **Space Complexity**: $O(n+m)$ — Auxiliary memory allocation bound.
