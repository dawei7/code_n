## General
Executes a SQL query for **Find Overbooked Employees** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O(M log M + E log E)$ — Operation count bound.
- **Space Complexity**: $O(M + E)$ — Auxiliary memory allocation bound.
