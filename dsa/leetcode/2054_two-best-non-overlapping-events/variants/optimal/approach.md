## General
Given a **0-indexed** 2D integer array of `events` where $\text{events}[i] = [\text{startTime}_{i}, \text{endTime}_{i}, \text{value}_{i}]$. The $$i^{\text{th}}$$ event starts at $\text{startTime}_{i}$_ and ends at $\text{en..., the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(n\log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
