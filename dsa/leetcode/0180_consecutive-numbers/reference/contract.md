## Function Contract

**Inputs**

`Logs(id, num)` contains the logged values, with increasing `id` defining their sequence.

**Return value**

Return one row per qualifying value under the column `ConsecutiveNums`, in any order.
