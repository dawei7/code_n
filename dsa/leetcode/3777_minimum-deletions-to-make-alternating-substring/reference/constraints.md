## Constraints

- $1 \leq N \leq 10^5$
- Every `s[i]` is either `'A'` or `'B'`.
- $1 \leq Q \leq 10^5$
- Each `queries[i]` has length `2` or `3`.
- A length-2 query has the form `[1, j]`.
- A length-3 query has the form `[2, l, r]`.
- $0 \leq j \leq N-1$
- $0 \leq l \leq r \leq N-1$
