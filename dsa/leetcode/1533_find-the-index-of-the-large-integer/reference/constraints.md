## Constraints

- $2 \leq N \leq 5 \cdot 10^5$.
- All values in `reader` are positive integers, with exactly one element strictly larger than the rest.
- At most 20 calls to `compareSub` are permitted.
