## Constraints

- $1 \le \texttt{timestamp} \le 2 \cdot 10^9$
- All method calls are supplied in chronological order, so timestamps increase monotonically.
- At most `300` total calls are made to `hit` and `getHits`.
