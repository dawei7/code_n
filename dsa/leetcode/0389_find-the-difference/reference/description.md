## Description

You are given strings `s` and `t`. String `t` was formed by shuffling all characters of `s` and inserting one additional letter at an arbitrary position.

Return the letter that was added.
