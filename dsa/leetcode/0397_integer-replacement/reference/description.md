## Description

Starting from a positive integer `n`, apply these operations:

- If `n` is even, replace it with `n / 2`.
- If `n` is odd, replace it with either `n + 1` or `n - 1`.

Return the minimum number of operations needed to reach `1`.
