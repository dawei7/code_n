## Constraints

- $1 \le \texttt{input.length} \le 10^4$
- `input` may contain lowercase or uppercase English letters, newlines `'\n'`, tabs `'\t'`, dots `'.'`, spaces, and digits.
- Every file and directory name has positive length.
