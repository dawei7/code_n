## General
Executes a SQL query for **Team Dominance by Pass Success** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(p log t + g log g)$ — Operation count bound.
- **Space Complexity**: $O(g)$ — Auxiliary memory allocation bound.
