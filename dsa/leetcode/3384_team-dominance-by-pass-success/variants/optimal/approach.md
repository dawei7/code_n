## General
Executes a SQL query for **Team Dominance by Pass Success** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O(p log t + g log g)$ — Operation count bound.
- **Space Complexity**: $O(g)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
