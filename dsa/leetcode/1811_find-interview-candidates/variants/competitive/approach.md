## General
Executes a SQL query for **Find Interview Candidates** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O(C\log C + U)$ — Operation count bound.
- **Space Complexity**: $O(C + U)$ — Auxiliary memory allocation bound.
