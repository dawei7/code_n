## General
Given a **0-indexed** array `mountain`. Your task is to find all the **peaks** in the `mountain` array, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(N)$ — Auxiliary memory allocation bound.
