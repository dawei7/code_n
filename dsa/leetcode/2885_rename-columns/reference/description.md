## Description

```

DataFrame `students`
+-------------+--------+
| Column Name | Type   |
+-------------+--------+
| id          | int    |
| first       | object |
| last        | object |
| age         | int    |
+-------------+--------+

```

Write a solution to rename the columns as follows:

<ul>
	<li>`id` to `student_id`</li>
	<li>`first` to `first_name`</li>
	<li>`last` to `last_name`</li>
	<li>`age` to `age_in_years`</li>
</ul>

The result format is in the following example.
