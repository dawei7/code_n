## General
Executes a SQL query for **The Number of Employees Which Report to Each Employee** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(E)$ — Operation count bound.
- **Space Complexity**: $O(M)$ — Auxiliary memory allocation bound.
