## General
Executes a SQL query for **Employees Project Allocation** using Common Table Expressions (CTEs), relational JOINs, window ranking functions.

## Complexity detail
- **Time Complexity**: $O(n log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
