## Description

Write a solution that reports the `firstName`, `lastName`, `city`, and `state` for every row in `Person`. When a person's `personId` has no row in `Address`, return `null` for that person's city and state.

The result rows may be returned in any order.
