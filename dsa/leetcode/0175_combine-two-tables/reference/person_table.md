## Person Table

| Column Name | Type |
|---|---|
| `personId` | int |
| `lastName` | varchar |
| `firstName` | varchar |

`personId` is the primary key, so its values are unique. Each row records one person's identifier, first name, and last name.
