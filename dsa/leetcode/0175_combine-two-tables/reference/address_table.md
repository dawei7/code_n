## Address Table

| Column Name | Type |
|---|---|
| `addressId` | int |
| `personId` | int |
| `city` | varchar |
| `state` | varchar |

`addressId` is the primary key, so its values are unique. Each row stores the city and state associated with a person's `personId`.
