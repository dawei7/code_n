## General
Given Table: `Person`, the database query executes a relational pipeline using relational `JOIN` operations to correlate matching records across tables.

## Complexity detail
- **Time Complexity**: $O(P + A)$ — Operation count bound.
- **Space Complexity**: $O(P + A)$ — Auxiliary memory allocation bound.
