## Note

- A word whose length is less than `k` cannot belong to any group and is ignored.
- Equal strings at different indices are separate words, so duplicates contribute separately to a group's size.
