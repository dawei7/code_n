## Function Contract

**Inputs**

- `low`: The inclusive lower bound as a decimal string.
- `high`: The inclusive upper bound as a decimal string.

**Return value**

Return the number of strobogrammatic integers from `low` through `high`, including either bound when applicable.
