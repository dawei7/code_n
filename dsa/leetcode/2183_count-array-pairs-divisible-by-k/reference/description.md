## Description

Given a **0-indexed** integer array `nums` of length `n` and an integer `k`, return *the **number of pairs*** `(i, j)` *such that:*

<ul>
	<li>`0 <= i < j <= n - 1` *and*</li>
	<li>`nums[i] * nums[j]` *is divisible by* `k`.</li>
</ul>
