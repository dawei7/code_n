## Constraints

- $1 \le \texttt{transactions.length} \le 8$
- `transactions[i].length == 3`
- $0 \le \texttt{from_i}, \texttt{to_i} < 12$
- $\texttt{from_i} \ne \texttt{to_i}$
- $1 \le \texttt{amount_i} \le 100$
