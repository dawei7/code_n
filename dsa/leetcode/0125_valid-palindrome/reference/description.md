## Description

A phrase is a palindrome when it reads identically forward and backward after every uppercase letter is converted to lowercase and every non-alphanumeric character is removed. Letters and digits are alphanumeric.

Given a string `s`, return `true` if its normalized form is a palindrome and `false` otherwise.
