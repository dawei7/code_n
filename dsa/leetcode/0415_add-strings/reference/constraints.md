## Constraints

- $1 \le \lvert\texttt{num1}\rvert, \lvert\texttt{num2}\rvert \le 10^4$
- `num1` and `num2` contain only decimal digits.
- Neither input has a leading zero unless that entire input is `"0"`.
