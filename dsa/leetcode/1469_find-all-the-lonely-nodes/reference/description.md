## Description

A node in a binary tree is **lonely** when it is the only child of its parent.
The root cannot be lonely because it has no parent.

Given the tree's `root`, return an array containing the values of all lonely
nodes. The values may appear in any order.
