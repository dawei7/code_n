## General
Implements the optimal algorithm for **Sort Integers by The Number of 1 Bits** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n \log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
