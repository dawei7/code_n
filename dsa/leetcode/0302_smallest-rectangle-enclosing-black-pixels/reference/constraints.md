## Constraints

- `m == image.length`
- `n == image[i].length`
- $1 \le m,n \le 100$
- Every `image[i][j]` is `'0'` or `'1'`.
- $0 \le x < m$
- $0 \le y < n$
- `image[x][y] == '1'`
- All black pixels form exactly one connected component.
