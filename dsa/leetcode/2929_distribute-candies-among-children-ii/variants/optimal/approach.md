## General
Given two positive integers `n` and `limit`, the algorithm solves **Distribute Candies Among Children II** directly.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
