## Description

The university's current departments appear in `Departments`, while each row of `Students` retains the `department_id` assigned to that student. A student's assignment is invalid when its recorded department ID has no row in the current department table.

Find every student enrolled in a department that no longer exists. Report the student-side ID and name; students whose recorded department still exists must not appear.
