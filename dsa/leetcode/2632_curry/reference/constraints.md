## Constraints

- $1 \le \text{inputs.length} \le 1000$.
- $0 \le \text{fn.length} \le 1000$.
- Total number of arguments across calls equals `fn.length`.
- Numerical arguments are between $0$ and $10^5$.
