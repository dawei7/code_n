## Hints

1. Use depth-first search while carrying both the current tree node and the current position in `arr`.
2. When the traversal reaches the final array position, also verify that the matching node is a leaf.
