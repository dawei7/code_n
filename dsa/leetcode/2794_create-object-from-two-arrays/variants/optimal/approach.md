## General
Executes JavaScript logic for **Create Object from Two Arrays** using idiomatic control flow. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(n + K)$ — Operation count bound.
- **Space Complexity**: $O(n + K)$ — Auxiliary memory allocation bound.
