{
  "schema_version": 1,
  "challenge_id": "lc_2647",
  "status": "verified",
  "method": "asymptotic_optimality",
  "required_time": "O(n^2)",
  "summary": "Every minimum answer contains ceil(n(n+3)/4) coordinate pairs, so writing the required output already matches the direct construction's quadratic work.",
  "optimality": {
    "symbol": "n",
    "lower_bound": "Omega(n^2)",
    "upper_bound": "O(n^2)",
    "argument": "A red-edge potential proves that every successful initial set has at least ceil(n(n+3)/4) members, and the four-row construction attains that bound. The result therefore has Theta(n^2) pairs in every valid minimum answer, while the reference emits each selected coordinate once."
  },
  "replacement_checks": [
    {
      "kind": "lower_bound_proof",
      "evidence": "For red set R, 2|R|-e(R) cannot increase during propagation; its all-red value forces at least ceil(n(n+3)/4) initial triangles."
    },
    {
      "kind": "upper_bound_proof",
      "evidence": "The repeating four-row pattern has exactly ceil(n(n+3)/4) coordinates and emits every coordinate with constant work."
    },
    {
      "kind": "boundary_property_cases",
      "evidence": "Regression validates coordinate legality, uniqueness, exact minimum cardinality, and complete propagation for alternate answers and representative four-row remainders."
    }
  ]
}
