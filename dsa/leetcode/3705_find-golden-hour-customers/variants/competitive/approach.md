## General
Executes a SQL query for **Find Golden Hour Customers** using GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O(R log R + C log C)$ — Operation count bound.
- **Space Complexity**: $O(R + C)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
