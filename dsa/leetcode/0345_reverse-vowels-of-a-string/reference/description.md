## Description

Given a string `s`, reverse the order of only its vowels and return the resulting string.

The vowels are `a`, `e`, `i`, `o`, and `u`. Each may occur more than once and may be uppercase or lowercase.
