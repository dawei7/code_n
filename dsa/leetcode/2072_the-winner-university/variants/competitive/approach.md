## General
Executes a SQL query for **The Winner University** using Common Table Expressions (CTEs).

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
