## General
Uses single-pass sequential iteration. Maintains hash map (`dict`) for $O(1)$ average lookup, priority queue (`heapq`) for dynamic minimum/maximum tracking.

## Complexity detail
- **Time Complexity**: $O((E+Q) log(E+Q))$ — Operation count bound.
- **Space Complexity**: $O(E+Q)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
