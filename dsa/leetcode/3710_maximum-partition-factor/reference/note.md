## Note

A singleton group has no intra-group pair and therefore contributes no distance to the minimum. When `n = 2`, both required groups must be singletons, so there is no intra-group pair anywhere; in that case, the partition factor is defined to be `0`.
