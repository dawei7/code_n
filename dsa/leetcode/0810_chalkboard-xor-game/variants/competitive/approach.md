## General
Given an array of integers `nums` represents the numbers written on a chalkboard, the algorithm solves **Chalkboard XOR Game** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
