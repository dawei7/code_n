## General
Given two strings `s` and `t`, the algorithm solves **Longest Common Prefix After at Most One Removal** directly.

## Complexity detail
- **Time Complexity**: $O(min(n, m))$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
