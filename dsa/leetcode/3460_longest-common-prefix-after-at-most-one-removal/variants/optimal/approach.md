## General
Implements the optimal algorithm for **Longest Common Prefix After at Most One Removal** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(min(n, m))$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
