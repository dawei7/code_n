## General
Implements the optimal algorithm for **Minimum Score by Changing Two Elements** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
