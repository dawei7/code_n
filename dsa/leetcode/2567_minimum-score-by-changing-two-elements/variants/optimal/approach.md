## General
Given an integer array `nums`, the algorithm solves **Minimum Score by Changing Two Elements** directly.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
