## General
Executes a SQL query for **Product Sales Analysis IV** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O((s + p) log s)$ — Operation count bound.
- **Space Complexity**: $O(s + p)$ — Auxiliary memory allocation bound.
