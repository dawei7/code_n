## General
Executes a SQL query for **Product Sales Analysis IV** using Common Table Expressions (CTEs), relational JOINs, window functions, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((s + p) log s)$ — Operation count bound.
- **Space Complexity**: $O(s + p)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
