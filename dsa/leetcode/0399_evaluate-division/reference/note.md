## Note

The input is valid: query evaluation never requires division by zero, and the supplied equations contain no contradiction.

A variable absent from every equation is undefined. Consequently, even a query dividing such a variable by itself cannot be determined and returns `-1.0`.
