## Constraints

- `1 <= compressedString.length <= 1000`
- `compressedString` contains lowercase and uppercase English letters and digits.
- Every single-character repetition count encoded in `compressedString` lies in `[1, 10^9]`.
- At most `100` calls are made to `next` and `hasNext`.
