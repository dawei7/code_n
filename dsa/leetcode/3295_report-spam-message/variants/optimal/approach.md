## General
Algorithm uses single-pass sequential scanning.

## Complexity detail
- **Time Complexity**: $O(m + b)$ — Operation count bound.
- **Space Complexity**: $O(b)$ — Auxiliary memory allocation bound.
