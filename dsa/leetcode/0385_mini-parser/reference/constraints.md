## Constraints

- $1 \le \texttt{s.length} \le 5 \cdot 10^4$
- `s` contains digits, square brackets `"[]"`, the minus sign `'-'`, and commas `','`.
- `s` is a valid `NestedInteger` serialization.
- Every represented integer lies in $[-10^6,10^6]$.
