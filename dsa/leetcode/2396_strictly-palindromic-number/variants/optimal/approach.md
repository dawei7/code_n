## General
Implements the optimal algorithm for **Strictly Palindromic Number** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
