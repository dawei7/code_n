## General
Executes a SQL query for **Daily Leads and Partners** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
