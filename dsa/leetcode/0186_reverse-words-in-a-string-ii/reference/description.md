## Description

Given a character array `s`, reverse the order of its words. A word is a consecutive sequence of non-space characters, and adjacent words in `s` are separated by exactly one space.

Perform the transformation in place without allocating extra space.
