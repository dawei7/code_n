## General
Given a **0-indexed** integer array `nums` of size `n`, find the **maximum difference** between $\text{nums}[i]$ and $\text{nums}[j]$ (i.e., $\text{nums}[j] - \text{nums}[i]$), such that $0 \le i < j < n$ and $\text{nums}[i..., the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
