## General
Executes a SQL query for **Queries Quality and Percentage** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(g)$ — Auxiliary memory allocation bound.
