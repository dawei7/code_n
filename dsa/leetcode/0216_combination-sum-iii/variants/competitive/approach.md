## General
Given Find all valid combinations of `k` numbers that sum up to `n` such that the following conditions are true:, the algorithm solves **Combination Sum III** directly.

## Complexity detail
- **Time Complexity**: $O(2^9 k)$ — Operation count bound.
- **Space Complexity**: $O(k)$ — Auxiliary memory allocation bound.
