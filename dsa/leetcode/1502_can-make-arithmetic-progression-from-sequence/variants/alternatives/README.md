# Future alternatives

This directory reserves stable package space for later reviewed approaches,
such as a deliberately naive teaching solution. A future branch must receive
its own `approach.md`, app-local solution, native source, and Accepted
`submission.json` before it is added to `solution_variants.json` or shown in
the application.
