## General
Executes a SQL query for **Flight Occupancy and Waitlist Analysis** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(F + P)$ — Operation count bound.
- **Space Complexity**: $O(F)$ — Auxiliary memory allocation bound.
