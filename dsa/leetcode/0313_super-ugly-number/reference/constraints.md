## Constraints

- $1 \le n \le 10^5$
- $1 \le \texttt{primes.length} \le 100$
- $2 \le \texttt{primes[i]} \le 1000$
- Every `primes[i]` is prime.
- All values in `primes` are unique and sorted in ascending order.
