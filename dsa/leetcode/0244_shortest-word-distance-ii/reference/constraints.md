## Constraints

- $1 \le \texttt{wordsDict.length} \le 3 \times 10^4$
- $1 \le \texttt{wordsDict[i].length} \le 10$
- Every entry of `wordsDict` consists only of lowercase English letters.
- Both query words occur in `wordsDict`.
- `word1 != word2`
- At most `5000` calls are made to `shortest`.
