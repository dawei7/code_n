## Description

You are given an integer array `nums` of length $n$. Every element lies in the inclusive range $[1, n]$, but some values in that range may be absent while others may occur more than once.

Return all integers in $[1, n]$ that do not appear in `nums`.
