## Constraints

- $2 \le n \le 10^5$.
- $-10^9 \le \texttt{nums[i]} \le 10^9$.
- `nums` is strictly increasing.
- $1 \le q \le 10^5$.
- Every entry of `queries` has the form `[l, r]`.
- $0 \le l,r < n$ for every query.
