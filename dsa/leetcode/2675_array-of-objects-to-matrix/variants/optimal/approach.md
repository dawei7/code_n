## General
Given Write a function that converts an array of objects `arr` into a matrix `m`, the JavaScript algorithm uses ES6 `Map`/`Set` collections for $O(1)$ fast key lookups, functional array iteration methods. Edge case handling: guards against empty inputs using array/string length checks.

## Complexity detail
- **Time Complexity**: $O(S + k log k + rk)$ — Operation count bound.
- **Space Complexity**: $O(S + rk)$ — Auxiliary memory allocation bound.
