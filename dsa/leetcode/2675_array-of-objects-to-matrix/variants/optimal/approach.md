## General
Executes JavaScript logic for **Array of Objects to Matrix** using ES6 Map/Set lookup structures, JavaScript array iteration methods. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(S + k log k + rk)$ — Operation count bound.
- **Space Complexity**: $O(S + rk)$ — Auxiliary memory allocation bound.
