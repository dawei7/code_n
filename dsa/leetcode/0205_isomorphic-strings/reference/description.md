## Description

Given two strings `s` and `t`, determine whether they are **isomorphic**.

The strings are isomorphic when replacing characters in `s` can produce `t`. Every occurrence of a particular source character must receive the same replacement, and the order of the characters must remain unchanged. Two different source characters cannot map to the same target character, although mapping a character to itself is allowed.
