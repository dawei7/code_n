## Constraints

- $2 \le \texttt{nums.length} \le 3 \times 10^4$
- $-2^{31} \le \texttt{nums[i]} \le 2^{31} - 1$
- Every integer in `nums` occurs exactly twice except for two integers that occur exactly once.
