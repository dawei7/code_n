## General
Executes a SQL query for **Find Drivers with Improved Fuel Efficiency** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(T log T + D log D)$ — Operation count bound.
- **Space Complexity**: $O(T + D)$ — Auxiliary memory allocation bound.
