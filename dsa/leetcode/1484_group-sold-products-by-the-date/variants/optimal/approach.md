## General
Executes a SQL query for **Group Sold Products By The Date** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R + P \log P)$ — Operation count bound.
- **Space Complexity**: $O(P)$ — Auxiliary memory allocation bound.
