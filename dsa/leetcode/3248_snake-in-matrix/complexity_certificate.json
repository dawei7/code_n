{
  "schema_version": 1,
  "challenge_id": "lc_3248",
  "status": "verified",
  "method": "bounded_domain",
  "required_time": "O(c)",
  "summary": "The board side is at most 10 and every legal trace contains at most 100 commands, too little scaling range for a stable runtime-class verdict.",
  "workload_bound": {
    "symbol": "c",
    "maximum": 100,
    "unit": "movement commands",
    "source_constraint": "2 <= n <= 10 and 1 <= commands.length <= 100"
  },
  "replacement_checks": [
    {
      "kind": "bounded_work_proof",
      "evidence": "The accepted implementation performs one fixed direction lookup and one integer addition per command."
    },
    {
      "kind": "boundary_property_cases",
      "evidence": "Regression exhausts bounded valid paths on small boards and checks deterministic valid traces through every legal side and command-length boundary against a coordinate oracle."
    }
  ]
}
