## General
Given an input string `s`, reverse the order of the **words**, the algorithm solves **Reverse Words in a String** directly.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
