## Hint

**Hint 1:** Model the choices with a dynamic-programming state `dp(pos, mask)`: let `pos` identify the current bike being considered for assignment and let `mask` encode the workers still available to receive a bike.
