## Countries Table

| Column Name | Type |
|---|---|
| `country_id` | `int` |
| `country_name` | `varchar` |

`country_id` is the primary key, so it uniquely identifies each row. Every row associates one country identifier with that country's name.
