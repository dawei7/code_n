## Constraints

- $-2^{31} \le \texttt{val} \le 2^{31}-1$
- At most $2 \cdot 10^5$ total calls are made to `insert`, `remove`, and `getRandom`.
- The set contains at least one element whenever `getRandom` is called.
