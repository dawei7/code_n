## Constraints

- $1 \le \lvert\texttt{nums}\rvert \le 10^5$
- $1 \le \texttt{nums[i]} \le 10^8$
- $1 \le \texttt{value} \le 10^8$
- At most 50,000 calls in total are made to `showFirstUnique` and `add`.
