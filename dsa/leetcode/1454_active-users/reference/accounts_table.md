## Accounts Table

`Accounts` stores the identity and name of each account.

| Column Name | Type |
|---|---|
| `id` | `int` |
| `name` | `varchar` |

The `id` column is the table's primary key, so every account ID is unique.
