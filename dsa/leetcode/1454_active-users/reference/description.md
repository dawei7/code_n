## Description

An account is an **active user** when it has login events on five or more
consecutive calendar days.

Find the `id` and `name` of every active user. Return the result rows ordered by
`id` in ascending order. The example below illustrates the required result
format.
