## Function Contract

**Inputs**

- `board`: A $9 \times 9$ grid whose cells contain a digit from `1` through `9` or `.` for an empty position.

**Return value**

Return `true` when every filled cell respects the row, column, and sub-box rules; otherwise return `false`.
