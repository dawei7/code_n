## Constraints

- `product_id` is the primary key for `Product`.
- `invoice_id` is the primary key for `Invoice`.
- `name` in `Product` consists of only lowercase English letters.
- No two products have the same `name`.
