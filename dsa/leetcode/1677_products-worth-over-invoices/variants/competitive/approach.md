## General
Executes a SQL query for **Product's Worth Over Invoices** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O(I + P \log P)$ — Operation count bound.
- **Space Complexity**: $O(P)$ — Auxiliary memory allocation bound.
