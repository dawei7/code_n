## General
Given This question is about implementing a basic elimination algorithm for Candy Crush, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(Rmn)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
