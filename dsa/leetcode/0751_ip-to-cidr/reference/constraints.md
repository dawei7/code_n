## Constraints

- `7 <= ip.length <= 15`
- `ip` is a valid IPv4 address in the form `"a.b.c.d"`, where `a`, `b`, `c`, and `d` are integers in `[0, 255]`.
- `1 <= n <= 1000`
- Every implied address `ip + x` for `x < n` is a valid IPv4 address.
