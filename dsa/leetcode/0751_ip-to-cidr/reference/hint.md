## Hint

Convert IP addresses to and from integer form. At the current start, the largest usable block is limited both by the lowest set bit of the integer address and by the greatest power of two no larger than the remaining count `n`. Choose the smaller limit, then repeat with the advanced start and reduced count.
