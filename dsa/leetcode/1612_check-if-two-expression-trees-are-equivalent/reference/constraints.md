## Constraints

- The number of nodes in both trees is in the range $[1, 4999]$.
- `Node.val` is either `'+'` or a lowercase English letter.
- Every internal node has `Node.val == '+'` and non-null left and right children.
- Every leaf node has `Node.val` as a lowercase English letter and null children.
