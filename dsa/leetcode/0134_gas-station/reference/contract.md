## Function Contract

**Inputs**

- `gas`: The gas available at each station.
- `cost`: The gas consumed on each station's outgoing clockwise leg.

**Return value**

Return the unique feasible zero-based starting index, or `-1` if no start can complete the circuit.
