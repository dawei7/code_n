## Constraints

- $0 \le \lvert \texttt{bank} \rvert \le 10$
- `startGene`, `endGene`, and every `bank[i]` have length $8$.
- `startGene`, `endGene`, and every `bank[i]` contain only `A`, `C`, `G`, and `T`.
