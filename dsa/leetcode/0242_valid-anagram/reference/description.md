## Description

Given two strings `s` and `t`, return `true` if `t` is an anagram of `s` and `false` otherwise. An anagram contains exactly the same characters with the same multiplicities, possibly in a different order.
