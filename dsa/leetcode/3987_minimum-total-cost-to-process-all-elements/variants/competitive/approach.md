## General
Given an integer array `nums` and an integer `k`, the algorithm solves **Minimum Total Cost to Process All Elements** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
