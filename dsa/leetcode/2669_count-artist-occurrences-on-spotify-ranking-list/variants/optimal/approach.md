## General
Executes a SQL query for **Count Artist Occurrences On Spotify Ranking List** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R log R)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
