## Constraints

- `keyboard.length == 26`
- `keyboard` contains each lowercase English letter exactly once, in some order.
- $1 \le \texttt{word.length} \le 10^4$
- Every `word[i]` is a lowercase English letter.
