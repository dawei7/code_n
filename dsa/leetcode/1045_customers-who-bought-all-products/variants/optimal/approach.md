## General
Executes a SQL query for **Customers Who Bought All Products** using GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O(R log R + Q)$ — Operation count bound.
- **Space Complexity**: $O(R+Q)$ — Auxiliary memory allocation bound.
