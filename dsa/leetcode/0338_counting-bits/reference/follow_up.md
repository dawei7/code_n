## Follow-up

- A straightforward approach can take $O(n\log n)$ time. Can you produce the answers in linear $O(n)$ time, possibly in one pass?
- Can you avoid built-in population-count operations such as C++'s `__builtin_popcount`?
