## Constraints

- `1 <= s.length <= 50`
- `s` contains only curly braces `'{}'`, commas `','`, and lowercase English letters.
- `s` is guaranteed to be a valid input.
- Curly braces are not nested.
- All characters between each consecutive opening and closing brace are distinct.
