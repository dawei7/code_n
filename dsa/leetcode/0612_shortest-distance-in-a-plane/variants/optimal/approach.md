## General
Executes a SQL query for **Shortest Distance in a Plane** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O(P^2)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
