## Point2D Table

| Column Name | Type |
|---|---|
| `x` | int |
| `y` | int |

The coordinate pair `(x, y)` is the table's primary key, so each stored point is unique. Every row gives one point's position in the Cartesian plane.
