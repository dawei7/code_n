## Constraints

- $1 \le \texttt{nums.length} \le 2 \cdot 10^4$
- $-2^{31} \le \texttt{nums[i]} \le 2^{31}-1$
- Every `target` is a value contained in `nums`.
- At most $10^4$ calls are made to `pick`.
