## General
Given Table: `Salary`, the database query executes a relational pipeline using Common Table Expressions (CTEs) to separate intermediate logic into modular subqueries, relational `JOIN` operations to correlate matching records across tables.

## Complexity detail
- **Time Complexity**: $O((S + E) \log(S + E))$ — Operation count bound.
- **Space Complexity**: $O(S + E)$ — Auxiliary memory allocation bound.
