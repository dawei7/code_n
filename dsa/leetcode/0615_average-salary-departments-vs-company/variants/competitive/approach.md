## General
Executes a SQL query for **Average Salary: Departments VS Company** using relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((S + E) \log(S + E))$ — Operation count bound.
- **Space Complexity**: $O(S + E)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
