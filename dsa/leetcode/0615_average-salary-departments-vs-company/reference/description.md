## Description

For every department represented in a payment month, compare that department's average salary with the whole company's average salary for the same month.

Return `higher`, `lower`, or `same` according to the comparison. Each result row identifies the month as `pay_month`, the department as `department_id`, and the outcome as `comparison`. The result may be returned in any order.
