## Salary Table

| Column Name | Type |
|---|---|
| `id` | int |
| `employee_id` | int |
| `amount` | int |
| `pay_date` | date |

In SQL, `id` is the primary key. Each row records one employee's salary payment for a month, and `employee_id` is a foreign key referencing `Employee`.
