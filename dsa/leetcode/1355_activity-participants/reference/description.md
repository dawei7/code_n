## Description

Each friend participates in the activity named by that friend's `Friends` row. The participant count of an activity is therefore the number of friend rows that name it. Every activity listed in `Activities` is guaranteed to be performed by at least one person in `Friends`.

Compare the participant counts across all activities. Return the names of the activities whose counts are neither the global maximum nor the global minimum. Every activity tied at either extreme is excluded.
