## General
Given Table: `Stadium`, the database query executes a relational database query for **Human Traffic of Stadium**.

## Complexity detail
- **Time Complexity**: $O(n \log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
