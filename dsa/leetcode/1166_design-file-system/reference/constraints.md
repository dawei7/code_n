## Constraints

- $2 \le \texttt{path.length} \le 100$
- $1 \le \texttt{value} \le 10^9$
- Every `path` is valid and contains only lowercase English letters and `/` characters.
- At most $10^4$ calls in total are made to `createPath` and `get`.
