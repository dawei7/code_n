## Constraints

- `user_id` is the primary key for the `Users` table.
- `trans_id` is the primary key for the `Transactions` table.
- `paid_by` and `paid_to` are valid `user_id` values from the `Users` table.
