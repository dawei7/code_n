## Function Contract

**Inputs**

- `citations`: Non-negative citation counts, one for each paper.

**Return value**

Return the greatest $h$ such that at least $h$ entries in `citations` are at least $h$.
