## General
Given A string can be **abbreviated** by replacing any number of **non-adjacent**, **non-empty** substrings with their lengths. The lengths **should not** have leading zeros, the algorithm solves **Valid Word Abbreviation** directly.

## Complexity detail
- **Time Complexity**: $O(w + a)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
