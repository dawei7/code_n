## Constraints

- $1 \le \lvert\texttt{word}\rvert \le 20$
- `word` contains only lowercase English letters.
- $1 \le \lvert\texttt{abbr}\rvert \le 10$
- `abbr` contains only lowercase English letters and decimal digits.
- Every integer represented in `abbr` fits in a signed 32-bit integer.
