## General
Given You are playing a game that has `n` levels numbered from `0` to $n - 1$. You are given a **0-indexed** integer array `damage` where $\text{damage}[i]$ is the amount of health you will lose to complete the $$i^{\text{t..., the algorithm solves **Minimum Health to Beat Game** directly.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
