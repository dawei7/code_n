## General
Executes a SQL query for **Rectangles Area** using relational JOINs.

## Complexity detail
- **Time Complexity**: $O(P^2 \log P)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
