## Points Table

`Points` stores identified points in a two-dimensional coordinate plane.

| Column Name | Type |
|---|---|
| `id` | `int` |
| `x_value` | `int` |
| `y_value` | `int` |

The `id` column contains unique values. Each row represents the point with
coordinates `(x_value, y_value)`.
