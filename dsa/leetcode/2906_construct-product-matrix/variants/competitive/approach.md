## General
Given a **0-indexed** 2D integer matrix `grid` of size $n * m$, we define a **0-indexed** 2D matrix `p` of size $n * m$ as the **product** matrix of `grid` if the following condition is met:, the algorithm executes a two-pointer approach to shrink boundaries or maintain a sliding window. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(N)$ — Auxiliary memory allocation bound.
