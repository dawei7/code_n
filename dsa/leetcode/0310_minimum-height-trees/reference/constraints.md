## Constraints

- $1 \le n \le 2 \cdot 10^4$
- `edges.length == n - 1`
- $0 \le a_i,b_i < n$
- $a_i \ne b_i$
- Every pair $(a_i,b_i)$ is distinct.
- The input is guaranteed to be a tree, with no repeated edges.
