## Student Table

| Column Name | Type |
|---|---|
| `student_id` | int |
| `student_name` | varchar |

`student_id` is the table's primary key, so it identifies exactly one student row. `student_name` stores that student's name.
