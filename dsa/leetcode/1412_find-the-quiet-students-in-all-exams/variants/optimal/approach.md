## General
Executes a SQL query for **Find the Quiet Students in All Exams** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations. Edge cases: filters aggregated group boundaries using `HAVING` clause.

## Complexity detail
- **Time Complexity**: $O(E \log E + S)$ — Operation count bound.
- **Space Complexity**: $O(E + S)$ — Auxiliary memory allocation bound.
