## General
Algorithm uses single-pass sequential scanning. Maintains dynamic programming memoization array/table.

## Complexity detail
- **Time Complexity**: $O(k \cdot m \cdot n)$ — Operation count bound.
- **Space Complexity**: $O(m \cdot n)$ — Auxiliary memory allocation bound.
