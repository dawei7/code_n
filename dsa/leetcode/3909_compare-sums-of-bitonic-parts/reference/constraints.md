## Constraints

- $3 \le n=\lvert\texttt{nums}\rvert \le 10^5$
- $1 \le \texttt{nums[i]} \le 10^9$
- `nums` is a bitonic array: it is strictly increasing up to a single peak and strictly decreasing afterward.
