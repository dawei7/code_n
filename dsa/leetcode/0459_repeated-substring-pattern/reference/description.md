## Description

Given a string `s`, determine whether it can be constructed by choosing one of its substrings and appending multiple identical copies of that substring together.

The repeated unit must be nonempty and shorter than the whole string, so at least two copies are required and no characters may remain outside the repeated blocks.
