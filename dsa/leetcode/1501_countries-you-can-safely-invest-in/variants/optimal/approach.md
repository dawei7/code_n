## General
Executes a SQL query for **Countries You Can Safely Invest In** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O((P + C + E) \log E)$ — Operation count bound.
- **Space Complexity**: $O(P + C + E)$ — Auxiliary memory allocation bound.
