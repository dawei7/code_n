## General
Given Table `Person`:, the database query executes a relational pipeline using relational `JOIN` operations to correlate matching records across tables, `GROUP BY` aggregations to summarize record groups.

## Complexity detail
- **Time Complexity**: $O((P + C + E) \log E)$ — Operation count bound.
- **Space Complexity**: $O(P + C + E)$ — Auxiliary memory allocation bound.
