## General
Executes a SQL query for **Countries You Can Safely Invest In** using relational JOINs, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((P + C + E) \log E)$ — Operation count bound.
- **Space Complexity**: $O(P + C + E)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
