## Description

Given an integer `year` and an integer `month`, return the number of days in that month of the specified year.

The month uses the usual calendar numbering from January as `1` through December as `12`. February depends on whether `year` is a leap year; the other month lengths do not vary by year. A leap century is divisible by 400, while a non-century leap year is divisible by 4.
