## Function Contract

**Inputs**

`Scores(id, score)` contains one game score per row and may contain ties.

**Return value**

Return a table with columns `score` and `rank`, retaining every input row and ordering the rows by score descending.
