## Scores Table

| Column Name | Type |
|---|---|
| `id` | int |
| `score` | decimal |

`id` is the primary key, so its values are unique. Each row stores a game's score as a floating-point value with two decimal places.
