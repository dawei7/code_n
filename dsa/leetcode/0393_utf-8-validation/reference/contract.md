## Function Contract

**Inputs**

- `data`: A nonempty sequence of integer byte values.

**Return value**

Return `true` if the complete byte sequence can be partitioned into valid one- through four-byte UTF-8 patterns; otherwise return `false`.
