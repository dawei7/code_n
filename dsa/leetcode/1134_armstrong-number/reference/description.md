## Description

Given an integer `n` containing $k$ decimal digits, determine whether it is an Armstrong number. Compute the $k$th power of each digit, including every repeated digit and any zero, and add those powered values.

The number is Armstrong if and only if that complete sum equals the original `n`. The exponent is the number of digits in the entire input, not a fixed exponent chosen from an example. Return the corresponding Boolean result.
