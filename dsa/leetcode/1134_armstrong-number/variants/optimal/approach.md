## General
Given an integer `n`, return `true` *if and only if it is an **Armstrong number***, the algorithm solves **Armstrong Number** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
