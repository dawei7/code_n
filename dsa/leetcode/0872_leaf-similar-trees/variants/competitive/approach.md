## General
Algorithm uses two-pointer sliding window iteration. Maintains tree node traversal (`val`, `left`, `right`). Edge cases: handles empty/null inputs via early return guards.

## Complexity detail
- **Time Complexity**: $O(n_1+n_2)$ — Operation count bound.
- **Space Complexity**: $O(h_1+h_2)$ — Auxiliary memory allocation bound.
