## Description

Given an equation, represented by `words` on the left side and the `result` on the right side.

You need to check if the equation is solvable under the following rules:

<ul>
	<li>Each character is decoded as one digit (0 - 9).</li>
	<li>No two characters can map to the same digit.</li>
	<li>Each `words[i]` and `result` are decoded as one number **without** leading zeros.</li>
	<li>Sum of numbers on the left side (`words`) will equal to the number on the right side (`result`).</li>
</ul>

Return `true` *if the equation is solvable, otherwise return* `false`.
