## Description

The string `s` contains the letters from English representations of decimal digits `0` through `9`, but those
letters appear out of order. Reconstruct every represented digit and return them as one string in ascending numeric
order.

The input is guaranteed to describe a valid multiset of digit names, and repeated digits must appear repeatedly in
the result.
