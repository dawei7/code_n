## Description

You are given an $m \times n$ integer matrix `mat`. Every row is sorted in strictly increasing order, so the same value cannot occur more than once within a single row. Different rows may share several values, but a value qualifies only when it is present in all $m$ rows.

Return the smallest element that occurs in every row. If the rows have no element in common, return `-1`.
