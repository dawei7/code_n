## Description

Given the `root` of a binary search tree, a numeric `target`, and an integer `k`, return the `k` values in the BST whose absolute distances from `target` are smallest. The result may list those values in any order.

The input guarantees that exactly one set of `k` tree values is closest to `target`.
