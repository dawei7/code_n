## Constraints

- $0 \le \lvert\texttt{intervals}\rvert \le 10^4$
- `intervals[i].length == 2`
- $0 \le \texttt{start_i} \le \texttt{end_i} \le 10^5$
- `intervals` is sorted by `start_i` in ascending order.
- `newInterval.length == 2`
- $0 \le \texttt{start} \le \texttt{end} \le 10^5$
