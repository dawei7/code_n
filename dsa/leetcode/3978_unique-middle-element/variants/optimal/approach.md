## General
Given an integer array `nums` of odd length `n`, the algorithm solves **Unique Middle Element** directly.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
