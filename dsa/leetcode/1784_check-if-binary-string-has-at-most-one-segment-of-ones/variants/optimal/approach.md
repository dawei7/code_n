## General
Implements the optimal algorithm for **Check if Binary String Has at Most One Segment of Ones** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
