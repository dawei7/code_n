## General
Uses breadth-first search queue traversal. Maintains priority queue (`heapq`) for dynamic minimum/maximum tracking.

## Complexity detail
- **Time Complexity**: $O(n log n + k log k)$ — Operation count bound.
- **Space Complexity**: $O(n + k)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
