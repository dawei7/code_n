## Description

Given the head of a sorted linked list, delete every node whose value occurs more than once. Only values that were distinct in the original list remain.

Return the resulting list, which stays sorted.
