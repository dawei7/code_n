## Constraints

- $1 \le n \le 20$.
- Every `points[i]` contains exactly three coordinates: $[x_i,y_i,z_i]$.
- $0 \le x_i,y_i,z_i \le 6$.
- `target.length == 3`.
- $0 \le \texttt{target[i]} \le 6$.
- The initial points contain no duplicate coordinate triples.
