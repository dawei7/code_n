## General
Given a **0-indexed** integer array `nums`. For each index `i` ($1 \le i \le \text{nums.length} - 2$) the **beauty** of $\text{nums}[i]$ equals:, the algorithm executes a two-pointer approach to shrink boundaries or maintain a sliding window.

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(N)$ — Auxiliary memory allocation bound.
