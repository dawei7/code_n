## General
Implements the optimal algorithm for **Minimum Common Value** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(n + m)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
