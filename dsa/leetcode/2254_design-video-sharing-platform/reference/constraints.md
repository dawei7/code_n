## Constraints

- $1 \le \text{video.length} \le 100$.
- `video` consists of digits.
- $0 \le \text{videoId} \le 10^5$.
- $0 \le \text{startMinute} < \text{endMinute} < 10^5$.
- At most $10^5$ total calls will be made across all methods.
