## General
Executes a SQL query for **Find Invalid IP Addresses** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(S + k log k)$ — Operation count bound.
- **Space Complexity**: $O(S + k)$ — Auxiliary memory allocation bound.
