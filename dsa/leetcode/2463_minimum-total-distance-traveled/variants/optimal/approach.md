## General
Given There are some robots and factories on the X-axis. You are given an integer array `robot` where $\text{robot}[i]$ is the position of the $$i^{\text{th}}$$ robot. You are also given a 2D integer array `factory` where $..., the algorithm executes depth-first search (DFS) recursion to explore valid decision branches.

## Complexity detail
- **Time Complexity**: $O(R\log R+F\log F+RF)$ — Operation count bound.
- **Space Complexity**: $O(R+F)$ — Auxiliary memory allocation bound.
