## Description

Given an integer array `nums` and an integer `k`, determine whether two **distinct indices** `i` and `j` satisfy both `nums[i] == nums[j]` and $\lvert i-j \rvert \le k$.

Return `true` if such a pair exists and `false` otherwise.
