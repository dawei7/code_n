## Constraints

- $1 \le \texttt{numCourses} \le 2000$
- $0 \le \texttt{prerequisites.length} \le \texttt{numCourses} \times (\texttt{numCourses} - 1)$
- `prerequisites[i].length == 2`
- $0 \le a_i, b_i < \texttt{numCourses}$
- $a_i \ne b_i$
- All pairs $[a_i,b_i]$ are distinct.
