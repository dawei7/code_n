## General
Given an integer `num`, return *a string of its **base 7** representation*, the algorithm solves **Base 7** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(\log |num|)$ — Operation count bound.
- **Space Complexity**: $O(\log |num|)$ — Auxiliary memory allocation bound.
