## Constraints

- $1 \le \text{names.length} == \text{columns.length} \le 10^4$.
- $1 \le \text{columns}[i] \le 10$.
- All strings in `names` are unique.
- Each `row` length is between 1 and 10.
- At most $10^4$ calls to `sel`, 2000 to `ins`, 2000 to `rmv`, and 500 to `exp`.
