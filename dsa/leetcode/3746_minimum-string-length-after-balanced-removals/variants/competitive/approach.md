## General
Given a string `s` consisting only of the characters `'a'` and `'b'`, the algorithm solves **Minimum String Length After Balanced Removals** directly.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
