## Description

An array is in **arithmetic progression** when every adjacent difference is equal: `arr[i + 1] - arr[i]` has the same value for each valid index $i$.

One value was removed from such an array. The removed value was neither the first nor the last value, so the original endpoints remain present. Given the remaining values in their original order as `arr`, return the value that was removed.
