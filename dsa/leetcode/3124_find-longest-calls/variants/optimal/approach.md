## General
Executes a SQL query for **Find Longest Calls** using Common Table Expressions (CTEs), relational JOINs, window ranking functions.

## Complexity detail
- **Time Complexity**: $O(c log c)$ — Operation count bound.
- **Space Complexity**: $O(c)$ — Auxiliary memory allocation bound.
