## Description

You are given an integer array `prices`, where `prices[i]` is one stock's price on day `i`, and an integer `k`.

Return the maximum profit obtainable with at most `k` transactions, meaning at most `k` purchases and at most `k` sales.
