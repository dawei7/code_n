## Weather Table

| Column Name | Type |
|---|---|
| `id` | int |
| `recordDate` | date |
| `temperature` | int |

`id` values are unique, no two rows have the same `recordDate`, and each row records one day's temperature.
