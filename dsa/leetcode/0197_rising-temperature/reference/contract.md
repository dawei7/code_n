## Function Contract

**Inputs**

`Weather(id, recordDate, temperature)` contains one record per represented date.

**Return value**

Return one `id` column containing dates that are warmer than yesterday, in any order.
