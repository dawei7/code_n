## General
Given A **subsequence** of a string is a new string that is formed from the original string by deleting some (can be none) of the characters without disturbing the relative positions of the remaining characters. (i.e., `"ac..., the algorithm solves **Shortest Way to Form String** directly.

## Complexity detail
- **Time Complexity**: $O(AS+T)$ — Operation count bound.
- **Space Complexity**: $O(AS)$ — Auxiliary memory allocation bound.
