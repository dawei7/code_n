## General
Executes a SQL query for **Capital Gain/Loss** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(N)$ — Operation count bound.
- **Space Complexity**: $O(K)$ — Auxiliary memory allocation bound.
