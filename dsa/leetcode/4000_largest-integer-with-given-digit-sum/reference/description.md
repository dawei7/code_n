## Description

You are given two non-negative integers `n` and `s`. Find the largest non-negative integer that uses at most `n` decimal digits and whose digits add up to exactly `s`.

The result may use fewer than `n` digits when that is unavoidable, as in the zero-sum case. If no integer can meet the requested digit sum within the digit limit, return `-1`; otherwise return the largest valid integer itself.
