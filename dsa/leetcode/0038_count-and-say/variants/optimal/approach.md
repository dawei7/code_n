## General
Given The **count-and-say** sequence is a sequence of digit strings defined by the recursive formula:, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(L_n)$ — Operation count bound.
- **Space Complexity**: $O(L_n)$ — Auxiliary memory allocation bound.
