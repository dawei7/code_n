## General
Algorithm uses single-pass sequential scanning. Applies bitwise operations (`&`, `|`, `^`, `<<`, `>>`) for fast bitmask state updates.

## Complexity detail
- **Time Complexity**: $O(L_n)$ — Operation count bound.
- **Space Complexity**: $O(L_n)$ — Auxiliary memory allocation bound.
