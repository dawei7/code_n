## Function Contract

**Inputs**

`Employee(id, name, salary, departmentId)` contains employees. `Department(id, name)` supplies their department names.

**Return value**

Return `Department`, `Employee`, and `Salary` for every employee tied at a department's highest salary.
