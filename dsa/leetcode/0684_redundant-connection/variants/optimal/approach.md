## General
Given In this problem, a tree is an **undirected graph** that is connected and has no cycles, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(N \alpha(N))$ — Operation count bound.
- **Space Complexity**: $O(N)$ — Auxiliary memory allocation bound.
