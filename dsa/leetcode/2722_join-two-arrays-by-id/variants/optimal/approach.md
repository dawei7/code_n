## General
Executes JavaScript logic for **Join Two Arrays by ID** using ES6 Map/Set lookup structures.

## Complexity detail
- **Time Complexity**: $O(N log N)$ — Operation count bound.
- **Space Complexity**: $O(N)$ — Auxiliary memory allocation bound.
