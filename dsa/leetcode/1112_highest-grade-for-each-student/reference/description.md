## Description

For every student represented in `Enrollments`, find the student's highest grade and the course in which it was earned. When more than one course has that same maximum grade, select the course with the smallest `course_id`.

Return the chosen `student_id`, `course_id`, and `grade` for every student. The result rows must be ordered by `student_id` in ascending order.
