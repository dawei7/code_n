## Employee Table

| Column Name | Type |
|---|---|
| `id` | int |
| `name` | varchar |
| `salary` | int |
| `managerId` | int |

`id` is the primary key, so its values are unique. Each row gives an employee's identifier, name, salary, and manager identifier.
