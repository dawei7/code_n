## General
Given two integers, `n` and `k`, along with two 2D integer arrays, `stayScore` and `travelScore`, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(kn^2)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
