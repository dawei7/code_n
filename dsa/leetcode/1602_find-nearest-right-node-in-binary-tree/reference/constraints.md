## Constraints

- The number of nodes in the tree is $n$ where $1 \le n \le 10^5$.
- $1 \le \text{Node.val} \le 10^5$.
- All values in the tree are distinct or identified by node reference.
- `u` is a guaranteed node in the binary tree.
