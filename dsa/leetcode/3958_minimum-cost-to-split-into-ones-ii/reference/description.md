## Description

You are given an integer `n`.

In one step, you can split an integer `x` into two positive integers `a` and `b` such that `a + b = x`. The cost of this step is `a * b`.

Return the **minimum** total cost required to split `n` into `n` ones.
