## Description

Given a string `s`, determine the length of its longest substring whose characters are all distinct.

A substring is contiguous: it keeps every character between its chosen endpoints. Characters may not be skipped as they could be in a subsequence. Return only the maximum length, not the substring itself.
