## Constraints

- The number of nodes in the tree is in the range $[2, 100]$.
- $-10^9 \le \text{Node.val} \le 10^9$.
- All `Node.val` are unique.
- `leaf` is guaranteed to be a leaf in the tree rooted at `root`.
