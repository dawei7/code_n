## Constraints

- $2 \le \lvert\texttt{nums}\rvert \le 10^5$
- $1 \le \texttt{nums[i]} \le 10^9$
- $0 \le \lvert\texttt{swaps}\rvert \le 10^5$
- `swaps[i] = [p_i, q_i]`.
- $0 \le p_i < q_i \le \lvert\texttt{nums}\rvert-1$
- `[p_i, q_i] != [p_j, q_j]` for distinct swap entries.
