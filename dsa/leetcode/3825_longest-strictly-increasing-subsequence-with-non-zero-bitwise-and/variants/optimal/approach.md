## General
Algorithm uses single-pass sequential scanning. Applies bitwise operations (`&`, `|`, `^`, `<<`, `>>`) for fast bitmask state updates.

## Complexity detail
- **Time Complexity**: $O(NB log N)$ — Operation count bound.
- **Space Complexity**: $O(NB)$ — Auxiliary memory allocation bound.
