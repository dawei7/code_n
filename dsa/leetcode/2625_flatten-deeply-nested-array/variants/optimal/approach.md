## General
Executes JavaScript logic for **Flatten Deeply Nested Array** using idiomatic control flow. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(V)$ — Operation count bound.
- **Space Complexity**: $O(V + D)$ — Auxiliary memory allocation bound.
