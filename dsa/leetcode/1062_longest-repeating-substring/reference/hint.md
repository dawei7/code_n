## Hint

**Hint 1:** Generate substrings in $O(N^2)$ time and use hashing to identify equal contents.

**Hint 2:** Among the substring hashes that occur more than once, retain the greatest associated substring length.
