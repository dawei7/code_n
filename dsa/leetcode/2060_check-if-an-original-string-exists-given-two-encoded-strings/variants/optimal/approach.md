## General
Given An original string, consisting of lowercase English letters, can be encoded by the following steps:, the algorithm executes a single-pass linear scan through input elements. Edge case handling: guards against empty/null inputs via early returns.

## Complexity detail
- **Time Complexity**: $O(n_1n_2B)$ — Operation count bound.
- **Space Complexity**: $O(n_1n_2B)$ — Auxiliary memory allocation bound.
