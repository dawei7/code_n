## General
Executes JavaScript logic for **Cache With Time Limit** using ES6 Map/Set lookup structures. Edge cases: guards against empty arrays/strings through length bounds.

## Complexity detail
- **Time Complexity**: $O(1)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
