## General
Given There are `n` kids with candies. You are given an integer array `candies`, where each $\text{candies}[i]$ represents the number of candies the $$i^{\text{th}}$$ kid has, and an integer `extraCandies`, denoting the num..., the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
