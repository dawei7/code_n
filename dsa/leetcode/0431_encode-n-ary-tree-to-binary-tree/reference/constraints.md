## Constraints

- The tree contains between $0$ and $10^4$ nodes, inclusive.
- $0 \le \texttt{Node.val} \le 10^4$
- The N-ary tree's height is at most $1000$.
- Do not keep codec state in class-member, global, or static variables; encoding and decoding must be stateless.
