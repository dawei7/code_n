## General
Executes a SQL query for **Popularity Percentage** using Common Table Expressions (CTEs).

## Complexity detail
- **Time Complexity**: $O(R log R)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
