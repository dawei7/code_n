## Product Table

| Column Name | Type |
|---|---|
| `product_id` | int |
| `product_name` | varchar |
| `unit_price` | int |

`product_id` is the primary key, so each product identifier is unique. Every row gives one product's name and unit price.
