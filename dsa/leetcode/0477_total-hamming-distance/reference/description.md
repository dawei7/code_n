## Description

The Hamming distance between two integers counts the binary positions at which their corresponding bits differ.

Given the integer array `nums`, consider every unordered pair of array positions and return the sum of their Hamming
distances. Each pair contributes once to the aggregate.
