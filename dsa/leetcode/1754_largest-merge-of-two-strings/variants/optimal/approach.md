## General
Implements the optimal algorithm for **Largest Merge Of Two Strings** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(S^2)$ — Operation count bound.
- **Space Complexity**: $O(S)$ — Auxiliary memory allocation bound.
