## General
Given an integer array `rewardValues` of length `n`, representing the values of rewards, the algorithm executes depth-first search (DFS) recursion to explore valid decision branches.

## Complexity detail
- **Time Complexity**: $O(n log n + nV / w)$ — Operation count bound.
- **Space Complexity**: $O(n + V / w)$ — Auxiliary memory allocation bound.
