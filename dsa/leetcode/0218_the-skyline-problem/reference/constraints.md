## Constraints

- $1 \le \texttt{buildings.length} \le 10^4$
- $0 \le \texttt{left}_i < \texttt{right}_i \le 2^{31} - 1$
- $1 \le \texttt{height}_i \le 2^{31} - 1$
- `buildings` is sorted by `left_i` in non-decreasing order.
