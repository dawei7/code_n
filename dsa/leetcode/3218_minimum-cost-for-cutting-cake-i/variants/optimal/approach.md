## General
Implements the optimal algorithm for **Minimum Cost for Cutting Cake I** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(m log m + n log n)$ — Operation count bound.
- **Space Complexity**: $O(m + n)$ — Auxiliary memory allocation bound.
