## Function Contract

**Inputs**

- `nums`: The distinct ascending array after a possible rotation.
- `target`: The value to locate.

Let $n = \lvert\texttt{nums}\rvert$.

**Return value**

Return the zero-based target index, or `-1` when `target` is absent.
