## Description

String `num` represents a nonnegative integer. Remove exactly `k` digit occurrences while preserving the relative order of those retained, and return the smallest possible resulting integer.

The returned representation must not contain leading zeroes. If every digit is removed or the remaining value is zero, return `"0"`.
