## General
Executes a SQL query for **The Most Recent Orders for Each Product** using Common Table Expressions (CTEs), relational JOINs, window ranking functions.

## Complexity detail
- **Time Complexity**: $O(r \log r)$ — Operation count bound.
- **Space Complexity**: $O(r)$ — Auxiliary memory allocation bound.
