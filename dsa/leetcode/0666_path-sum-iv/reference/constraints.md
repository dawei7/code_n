## Constraints

- The array length satisfies $1 \leq \lvert\texttt{nums}\rvert \leq 15$.
- Every encoded integer `nums[i]` is between `110` and `489`, inclusive.
- `nums` represents a valid binary tree whose depth is less than `5`.
- `nums` is sorted in ascending order.
