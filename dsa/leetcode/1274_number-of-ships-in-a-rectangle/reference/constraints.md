## Constraints

- The input named `ships` is used only to initialize the hidden map. Its positions are not visible to the solution, which must obtain the answer through the `hasShips` API.
- `0 <= bottomLeft[0] <= topRight[0] <= 1000`
- `0 <= bottomLeft[1] <= topRight[1] <= 1000`
- `topRight != bottomLeft`
