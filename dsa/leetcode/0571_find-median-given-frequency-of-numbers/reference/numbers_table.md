## Numbers Table

| Column Name | Type |
|---|---|
| `num` | int |
| `frequency` | int |

`num` is the primary key, so every represented number has exactly one row. Each row records how often that number occurs in the database.
