## Description

The median is the value that divides a data sample into a lower half and a higher half. The `Numbers` table stores that sample in compressed form: a row `(num, frequency)` represents `frequency` occurrences of `num`.

Report the median of the fully decompressed collection without requiring that collection to be materialized. Round the result to one decimal place.
