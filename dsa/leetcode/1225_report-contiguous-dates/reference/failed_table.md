## Failed Table

| Column Name | Type |
|---|---|
| `fail_date` | date |

`fail_date` is the table's primary key, so every stored date is unique. Each row identifies a day on which the system's task failed.
