## Constraints

- $1 \le \text{dict.length} \le 10^5$.
- $1 \le \text{dict}[i].length \le 20$.
- All strings in `dict` have the same length.
- Total characters across all strings in `dict` $\le 10^5$.
- All strings in `dict` consist of lowercase English letters and are distinct.
