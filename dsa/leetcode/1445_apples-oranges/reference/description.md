## Description

Summarize the fruit sales recorded for each date. For every distinct `sale_date`, subtract the number of oranges sold from the number of apples sold and report that signed difference as `diff`.

Return one row per date with columns `sale_date` and `diff`. Sort the result by `sale_date` in ascending chronological order. A result may be positive, zero, or negative depending on which fruit sold more units that day.
