## Sales Table

| Column Name | Type |
|---|---|
| `sale_date` | date |
| `fruit` | enum |
| `sold_num` | int |

The composite primary key is (`sale_date`, `fruit`). The table records the daily numbers of `apples` and `oranges` sold.
