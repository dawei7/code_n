## General
Implements the optimal algorithm for **Reshape Data: Pivot** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(r log r + cm)$ — Operation count bound.
- **Space Complexity**: $O(cm)$ — Auxiliary memory allocation bound.
