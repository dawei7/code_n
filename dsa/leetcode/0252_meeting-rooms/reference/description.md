## Description

The array `intervals` contains meeting times, with `intervals[i] = [start_i, end_i]`.

One person can attend every meeting only when no two intervals overlap. A meeting ending at time $t$ is compatible with another beginning at the same time $t$; those intervals do not overlap.

Return `true` if all meetings can be attended and `false` otherwise.
