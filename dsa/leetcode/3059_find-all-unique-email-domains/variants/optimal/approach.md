## General
Executes a SQL query for **Find All Unique Email Domains** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(S + g log g)$ — Operation count bound.
- **Space Complexity**: $O(g)$ — Auxiliary memory allocation bound.
