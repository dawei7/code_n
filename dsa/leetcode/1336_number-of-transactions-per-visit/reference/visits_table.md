## Visits Table

| Column Name | Type |
|---|---|
| `user_id` | `int` |
| `visit_date` | `date` |

The pair (`user_id`, `visit_date`) is the primary key. Each row records that the identified user visited the bank on that date.
