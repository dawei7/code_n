## Description

A second-degree follower is a user who both follows at least one other user and is followed by at least one user.

Report every second-degree follower together with the number of users who follow them. Name the two output columns `follower` and `num`, and order the rows alphabetically by `follower`.
