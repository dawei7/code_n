## General
Given Table: `Scores`, the database query executes a relational database query for **Running Total for Different Genders**.

## Complexity detail
- **Time Complexity**: $O(n\log n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
