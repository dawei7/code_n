## Cinema Table

| Column Name | Type |
|---|---|
| `seat_id` | int |
| `free` | bool |

`seat_id` is an auto-incrementing column. Each row describes the $i$th seat: `free = 1` means available and `free = 0` means occupied.
