## Description

Find every available seat that belongs to a consecutive group of available seats. Two seats are consecutive when their identifiers differ by one.

Return the qualifying `seat_id` values in ascending order. The generated test cases contain more than two consecutively available seats.
