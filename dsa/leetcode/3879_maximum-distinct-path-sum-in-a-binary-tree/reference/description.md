## Description

You are given the root of a binary tree in which every node stores an integer value.

A valid path is a sequence of connected nodes. Its two endpoints may be any nodes in the tree, and the path is not required to include the root. Every value appearing along the path must be distinct, even when equal values belong to different nodes.

Among all valid non-empty paths, return the greatest possible sum of the included node values.
