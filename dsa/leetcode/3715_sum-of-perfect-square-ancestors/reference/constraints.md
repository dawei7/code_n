## Constraints

- $1 \le n \le 10^5$
- `edges.length == n - 1`
- Every edge has the form `edges[i] = [u_i, v_i]`.
- $0 \le u_i,v_i \le n-1$
- `nums.length == n`
- $1 \le \texttt{nums[i]} \le 10^5$
- `edges` represents a valid tree.
