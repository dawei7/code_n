## Hint

- Compare `query(a, b, c, d)` with `query(a, b, c, e)`. If the query outputs are equal, then `nums[d] == nums[e]`, otherwise `nums[d] != nums[e]`. Use this property to divide all elements into two groups relative to a reference element.
