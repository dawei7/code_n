## Description

Given a node in a binary search tree, return that node's inorder successor. The successor is the node with the
smallest key strictly greater than `node.val`. Return `null` when no such node exists.

The tree root is not supplied separately. Instead, every node has a `parent` reference in addition to its `left` and
`right` child references, and the function receives direct access to the selected node.
