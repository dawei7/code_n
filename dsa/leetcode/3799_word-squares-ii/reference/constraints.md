## Constraints

- $4 \leq \lvert\texttt{words}\rvert \leq 15$
- $lvert\texttt{words[i]}\rvert = 4$
- Every string in `words` consists only of lowercase English letters.
- All strings in `words` are distinct.
