## General
Given an integer `eventTime` denoting the duration of an event, where the event occurs from time $t = 0$ to time $t = eventTime$, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
