## General
Algorithm uses single-pass sequential scanning. Maintains hash map lookup (`dict`) for $O(1)$ average speed, dynamic programming memoization array/table.

## Complexity detail
- **Time Complexity**: $O(n^2 + W)$ — Operation count bound.
- **Space Complexity**: $O(n + W)$ — Auxiliary memory allocation bound.
