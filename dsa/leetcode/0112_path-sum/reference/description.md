## Description

Given the `root` of a binary tree and an integer `targetSum`, determine whether the tree contains a root-to-leaf path whose node values add up to `targetSum`.

A leaf is a node with no children.
