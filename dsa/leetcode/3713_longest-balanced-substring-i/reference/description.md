## Description

You are given a string `s` containing lowercase English letters.

A substring is **balanced** when every distinct character that occurs within that substring has the same frequency.

Return the length of the longest balanced substring of `s`.
