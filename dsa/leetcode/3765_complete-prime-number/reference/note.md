## Note

- A **prefix** consists of the first `k` decimal digits of the number.
- A **suffix** consists of the last `k` decimal digits of the number.
- For a one-digit number, that digit is the only prefix and the only suffix. Such a number is Complete Prime exactly when it is prime.
