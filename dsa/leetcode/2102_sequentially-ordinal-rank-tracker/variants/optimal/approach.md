## General
Given A scenic location is represented by its `name` and attractiveness `score`, where `name` is a **unique** string among all locations and `score` is an integer. Locations can be ranked from the best to the worst. The **h..., the algorithm solves **Sequentially Ordinal Rank Tracker** directly.

## Complexity detail
- **Time Complexity**: $O(\log m)$ — Operation count bound.
- **Space Complexity**: $O(m)$ — Auxiliary memory allocation bound.
