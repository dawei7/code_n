## General
Given Convert a non-negative integer `num` to its English words representation, the algorithm solves **Integer to English Words** directly. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(\log num)$ — Operation count bound.
- **Space Complexity**: $O(\log num)$ — Auxiliary memory allocation bound.
