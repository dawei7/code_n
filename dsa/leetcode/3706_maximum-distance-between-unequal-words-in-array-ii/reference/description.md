## Description

You are given an array of strings named `words`. Choose two distinct positions $i$ and $j$ whose stored words are unequal.

For $i<j$, the distance between those positions is the inclusive span $j-i+1$. Find the largest such distance over every valid pair. Return `0` when no two positions contain different words.
