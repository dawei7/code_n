## General
Executes a SQL query for **CEO Subordinate Hierarchy** using Common Table Expressions (CTEs), relational JOINs.

## Complexity detail
- **Time Complexity**: $O(e log e)$ — Operation count bound.
- **Space Complexity**: $O(e)$ — Auxiliary memory allocation bound.
