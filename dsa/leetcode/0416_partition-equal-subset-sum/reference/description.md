## Description

Given an integer array `nums`, determine whether all of its element occurrences can be partitioned into two subsets
whose element sums are equal.

Return `true` when such a partition exists and `false` otherwise. The subsets need not occupy contiguous positions,
and duplicate values remain separate occurrences.
