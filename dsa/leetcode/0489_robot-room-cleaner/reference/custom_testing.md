## Custom Testing

The custom input supplies `room`, `row`, and `col` only so the judge can initialize the hidden environment and the
robot's position. The solution does not receive those values and must clean the room using only the four robot
operations, as though the layout and starting coordinates were hidden.
