## Note

- The permitted operators are `+`, `-`, `*`, and `/`.
- An operand may be an integer or the result of another expression.
- Integer division truncates toward zero.
- The expression never divides by zero.
- The supplied tokens always form a valid Reverse Polish Notation expression.
- Every intermediate value and the final answer fit in a signed 32-bit integer.
