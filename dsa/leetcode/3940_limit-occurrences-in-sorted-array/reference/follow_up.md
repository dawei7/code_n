## Follow-up

Can the filtering be performed in place with $O(1)$ auxiliary space?

When evaluating this bound, do not count the storage used to return or resize the result; some languages cannot resize an array in place.
