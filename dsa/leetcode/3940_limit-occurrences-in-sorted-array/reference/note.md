## Note

The upper bound does not permit discarding arbitrary additional copies. If a distinct value occurs at least `k` times in `nums`, the result must contain exactly `k` of those occurrences. A value occurring fewer than `k` times is retained completely.
