## Description

Given a binary search tree, find the lowest common ancestor (**LCA**) of two specified nodes `p` and `q`.

The LCA is the lowest node in the tree that has both targets as descendants. A node counts as its own descendant, so one of the targets may itself be the answer.
