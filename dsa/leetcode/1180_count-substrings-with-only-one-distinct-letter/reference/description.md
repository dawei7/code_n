## Description

Given a string `s`, count all of its substrings that contain exactly one distinct letter. A substring is a non-empty contiguous interval of `s`, and two equal pieces of text at different index intervals are separate occurrences.

Every one-character interval qualifies. A longer interval qualifies precisely when every character inside it is the same. Return the total number of qualifying substring occurrences across every possible start and end position.
