## Constraints

- $1 \le \lvert\texttt{words}\rvert \le 1000$
- $1 \le \lvert\texttt{words[i]}\rvert \le 4$
- All `words[i]` have the same length.
- Every `words[i]` contains only lowercase English letters.
- All `words[i]` are unique.
