## Constraints

- $0 \le \lvert \texttt{s} \rvert \le 300$
- `s` contains lowercase and uppercase English letters, digits, or characters from `"!@#$%^&*()_+-=',.:"`.
- The only space character that can occur in `s` is `' '`.
