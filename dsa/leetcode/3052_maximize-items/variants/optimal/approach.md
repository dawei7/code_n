## General
Executes a SQL query for **Maximize Items** using Common Table Expressions (CTEs), relational JOINs. Edge cases: replaces NULL values using `COALESCE` guard.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
