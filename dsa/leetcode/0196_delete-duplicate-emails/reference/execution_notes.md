## Execution Notes

- SQL submissions must execute a `DELETE` statement rather than merely selecting the desired rows.
- pandas submissions must modify `Person` in place.
- The driver runs the submitted code and then displays the resulting `Person` table.
