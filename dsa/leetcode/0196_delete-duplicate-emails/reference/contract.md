## Function Contract

**Inputs**

`Person(id, email)` is the table to mutate.

**Return value**

Modify `Person` so each email remains exactly once at its smallest original `id`. The final row order does not matter.
