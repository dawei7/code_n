## General
Given Table: `Person`, the database query executes a relational database query for **Delete Duplicate Emails**.

## Complexity detail
- **Time Complexity**: $O(n^2)$ — Operation count bound.
- **Space Complexity**: $O(n)$ — Auxiliary memory allocation bound.
