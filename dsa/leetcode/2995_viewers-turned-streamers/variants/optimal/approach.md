## General
Executes a SQL query for **Viewers Turned Streamers** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R log R)$ — Operation count bound.
- **Space Complexity**: $O(R)$ — Auxiliary memory allocation bound.
