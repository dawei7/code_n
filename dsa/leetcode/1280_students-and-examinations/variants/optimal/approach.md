## General
Executes a SQL query for **Students and Examinations** using relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(R+E)$ — Operation count bound.
- **Space Complexity**: $O(R+E)$ — Auxiliary memory allocation bound.
