## Players Table

The `Players` table has the following schema:

| Column Name | Type |
|---|---|
| `player_id` | int |
| `group_id` | int |

`player_id` is the table's primary key, so every player identifier is unique. Each row assigns that player to one tournament group.
