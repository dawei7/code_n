## General
Executes a SQL query for **Tournament Winners** using Common Table Expressions (CTEs), relational JOINs, window functions, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O(p+m)$ — Operation count bound.
- **Space Complexity**: $O(p+m)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
