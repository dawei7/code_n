## General
Given Implement a SnapshotArray that supports the following interface:, the algorithm executes a single-pass linear scan through input elements.

## Complexity detail
- **Time Complexity**: $O(\log u)$ — Operation count bound.
- **Space Complexity**: $O(\texttt{length}+s)$ — Auxiliary memory allocation bound.
