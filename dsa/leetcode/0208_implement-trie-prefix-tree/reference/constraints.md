## Constraints

- $1 \le \texttt{word.length}, \texttt{prefix.length} \le 2000$
- `word` and `prefix` contain only lowercase English letters.
- At most $3 \times 10^4$ calls in total are made to `insert`, `search`, and `startsWith`.
