## Description

Given a screen with `rows` rows and `cols` columns and a `sentence` represented as a list of words, determine how
many complete repetitions of that sentence fit on the screen.

Words must keep their original order, a word cannot be split between two rows, and exactly one space must separate
consecutive words placed on the same row.
