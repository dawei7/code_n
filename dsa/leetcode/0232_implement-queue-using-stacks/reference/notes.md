## Notes

- Use only standard stack operations: push to the top, inspect or pop from the top, obtain the size, and test whether the stack is empty.
- If the language has no native stack, a list or deque may simulate one, provided only those stack operations are used.
