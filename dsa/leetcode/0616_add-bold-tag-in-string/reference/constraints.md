## Constraints

- $1 \le N \le 1000$.
- $0 \le \lvert\texttt{words}\rvert \le 100$.
- For every `word` in `words`, $1 \le \lvert\texttt{word}\rvert \le 1000$.
- `s` and every value in `words` contain only English letters and digits.
- All values in `words` are unique.
