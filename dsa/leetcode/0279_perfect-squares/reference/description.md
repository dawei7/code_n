## Description

Given an integer `n`, return the minimum number of perfect-square values whose sum is exactly `n`.

A perfect square is an integer obtained by multiplying some integer by itself. Thus, `1`, `4`, `9`, and `16` are perfect squares, whereas `3` and `11` are not.
