## General
Algorithm uses single-pass sequential scanning. Maintains hash map lookup (`dict`) for $O(1)$ average speed.

## Complexity detail
- **Time Complexity**: $O(n_2+a+cn_1)$ — Operation count bound.
- **Space Complexity**: $O(n_1+n_2)$ — Auxiliary memory allocation bound.
