## Follow-up

Using $O(mn)$ auxiliary space is a straightforward but undesirable solution. $O(m+n)$ additional space is an improvement, though still not optimal. Can you solve the problem with constant extra space?
