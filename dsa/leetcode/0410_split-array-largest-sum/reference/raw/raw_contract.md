## Function Contract

**Inputs**

- `nums`: The nonnegative integer array to divide into contiguous groups.
- `k`: The exact number of non-empty subarrays required in the split.

**Return value**

Return the smallest achievable value of the largest subarray sum among all valid splits into `k` groups.
