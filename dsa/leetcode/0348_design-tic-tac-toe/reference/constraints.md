## Constraints

- $2 \le n \le 100$
- `player` is `1` or `2`.
- $0 \le \texttt{row},\texttt{col} < n$
- Each `(row,col)` pair is unique across calls to `move`.
- At most $n^2$ calls are made to `move`.
