## Description

An undirected graph has `n` nodes labeled from `0` through `n - 1`. Each pair `edges[i] = [a_i, b_i]` places an undirected edge between nodes $a_i$ and $b_i$.

Return `true` if these edges make the graph a valid tree, and return `false` otherwise.
