## Description

Given an integer array `nums` sorted in non-decreasing order, remove duplicates in place so that each distinct value appears exactly once while preserving relative order.

Let $k$ be the number of distinct values. Store those values in sorted order in the first $k$ positions of `nums`, return $k$, and disregard every position after index $k-1$.
