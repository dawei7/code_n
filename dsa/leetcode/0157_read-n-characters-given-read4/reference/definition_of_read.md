## Definition of read

- Parameters: `char[] buf, int n`
- Return type: `int`

`buf` is a destination with enough capacity for the requested characters. The implementation must write the result into it rather than reading initial data from it.
