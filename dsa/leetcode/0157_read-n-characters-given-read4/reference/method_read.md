## Method read

Implement `read(buf, n)` using only `read4`; the file itself cannot be accessed or repositioned directly. Copy no more than `n` characters into the destination `buf`, stop early at end of file, and return the number actually copied.
