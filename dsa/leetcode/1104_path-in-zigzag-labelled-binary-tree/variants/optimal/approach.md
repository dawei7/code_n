## General
Applies bitwise operations (`&`, `|`, `^`, `<<`, `>>`) for fast bitmask state updates.

## Complexity detail
- **Time Complexity**: $O(\log L)$ — Operation count bound.
- **Space Complexity**: $O(\log L)$ — Auxiliary memory allocation bound.
