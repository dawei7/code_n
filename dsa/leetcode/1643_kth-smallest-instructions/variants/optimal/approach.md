## General
Algorithm uses single-pass sequential scanning.

## Complexity detail
- **Time Complexity**: $O(r+c)$ — Operation count bound.
- **Space Complexity**: $O(r+c)$ — Auxiliary memory allocation bound.
