## Constraints

- $1 \leq m = \texttt{s.length} \leq 10^5$
- `s` contains only decimal digits.
- $1 \leq \texttt{queries.length} \leq 10^5$
- Every entry has the form `queries[i] = [l_i, r_i]`.
- $0 \leq l_i \leq r_i < m$
