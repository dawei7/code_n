## Constraints

- $1 \le \lvert\texttt{num1}\rvert, \lvert\texttt{num2}\rvert \le 200$
- `num1` and `num2` contain decimal digits only.
- Neither string has a leading zero unless the entire number is `"0"`.
