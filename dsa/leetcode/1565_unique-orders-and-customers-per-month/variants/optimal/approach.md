## General
Executes a SQL query for **Unique Orders and Customers Per Month** using GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(r \log r)$ — Operation count bound.
- **Space Complexity**: $O(r)$ — Auxiliary memory allocation bound.
