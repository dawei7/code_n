## Constraints

- Each linked list contains between $1$ and $100$ nodes, inclusive.
- $0 \le \texttt{Node.val} \le 9$
- Each list represents a number without leading zeros, except that zero itself is represented by `[0]`.
