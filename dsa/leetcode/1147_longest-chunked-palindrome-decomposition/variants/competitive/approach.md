## General
Given a string `text`. You should split it to k substrings $(\text{subtext}_{1}, \text{subtext}_{2}, ..., \text{subtext}_{k})$ such that:, the algorithm executes a two-pointer approach to shrink boundaries or maintain a sliding window. Edge case handling: applies modulo arithmetic to prevent integer overflow.

## Complexity detail
- **Time Complexity**: $O(n)$ — Operation count bound.
- **Space Complexity**: $O(1)$ — Auxiliary memory allocation bound.
