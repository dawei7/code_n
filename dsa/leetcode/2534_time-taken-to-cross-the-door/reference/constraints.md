## Constraints

- $n == \text{arrival.length} == \text{state.length}$.
- $1 \le n \le 10^5$.
- $0 \le \text{arrival}[i] \le n$.
- `arrival` is sorted in non-decreasing order.
- `state[i]` is either `0` or `1`.
