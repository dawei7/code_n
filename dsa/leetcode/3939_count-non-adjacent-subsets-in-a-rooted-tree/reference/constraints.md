## Constraints

- `n == parent.length == nums.length`
- $1\le n\le1000$
- `parent[0] == -1`
- For every $1\le i<n$, $0\le\texttt{parent[i]}<i$.
- $1\le\texttt{nums[i]}\le10^9$
- $1\le k\le100$
- `parent` describes a valid rooted tree.
