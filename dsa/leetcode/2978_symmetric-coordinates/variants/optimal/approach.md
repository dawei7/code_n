## General
Executes a SQL query for **Symmetric Coordinates** using Common Table Expressions (CTEs), relational JOINs, window ranking functions.

## Complexity detail
- **Time Complexity**: $O(R + D log(D))$ — Operation count bound.
- **Space Complexity**: $O(D)$ — Auxiliary memory allocation bound.
