## General
Executes a SQL query for **Find Books with No Available Copies** using Common Table Expressions (CTEs), relational JOINs, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(B log B + R log R)$ — Operation count bound.
- **Space Complexity**: $O(B + R)$ — Auxiliary memory allocation bound.
