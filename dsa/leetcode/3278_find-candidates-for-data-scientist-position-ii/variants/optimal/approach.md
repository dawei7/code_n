## General
Executes a SQL query for **Find Candidates for Data Scientist Position II** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O((C + P + J) log(C + P + J))$ — Operation count bound.
- **Space Complexity**: $O(C + P + J)$ — Auxiliary memory allocation bound.
