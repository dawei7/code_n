## General
Executes a SQL query for **Find Candidates for Data Scientist Position II** using Common Table Expressions (CTEs), relational JOINs, window functions, GROUP BY aggregations (`COALESCE`, `STRING_AGG`).

## Complexity detail
- **Time Complexity**: $O((C + P + J) log(C + P + J))$ — Operation count bound.
- **Space Complexity**: $O(C + P + J)$ — Auxiliary memory allocation bound.

## Alternatives and edge cases
- **Algorithm design:** Describes the specific algorithmic approach used in the solution.
- **Complexity bounds:** Declares the precise time and space complexity guarantees.
