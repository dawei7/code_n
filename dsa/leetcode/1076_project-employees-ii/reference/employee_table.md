## Employee Table

| Column Name | Type |
|---|---|
| `employee_id` | int |
| `name` | varchar |
| `experience_years` | int |

`employee_id` is the primary key, so its values are unique. Each row stores information about one employee.
