## Function Contract

**Inputs**

- `wordsDict`: A string array containing the required target occurrences.
- `word1`: The first target word.
- `word2`: The second target word, which may equal `word1`.

**Return value**

Return the minimum absolute index difference between two valid, distinct target occurrences.
