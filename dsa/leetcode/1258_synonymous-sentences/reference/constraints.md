## Constraints

- $0 \le \texttt{synonyms.length} \le 10$
- `synonyms[i].length == 2`
- $1 \le \lvert s_i \rvert, \lvert t_i \rvert \le 10$
- $s_i \ne t_i$
- `text` contains at most `10` words.
- Every pair in `synonyms` is unique.
- Words in `text` are separated by single spaces.
