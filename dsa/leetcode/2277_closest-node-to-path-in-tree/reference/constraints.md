## Constraints

- $1 \le n \le 1000$.
- $\text{edges.length} == n - 1$.
- $0 \le u_i, v_i < n$.
- $1 \le \text{query.length} \le 1000$.
- $0 \le \text{start}_i, \text{end}_i, \text{node}_i < n$.
- `edges` forms a valid tree.
