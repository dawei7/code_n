## General
Implements the optimal algorithm for **Find K Closest Elements** with natural boundary handling.

## Complexity detail
- **Time Complexity**: $O(\log(N - K) + K)$ — Operation count bound.
- **Space Complexity**: $O(K)$ — Auxiliary memory allocation bound.
