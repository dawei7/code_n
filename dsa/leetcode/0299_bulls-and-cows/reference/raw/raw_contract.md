## Function Contract

**Inputs**

- `secret`: The secret decimal-digit string.
- `guess`: The equally long decimal-digit string supplied by the other player.

**Return value**

Return a string formatted as `"xAyB"`, using the number of correct-position digits for `x` and the number of additional wrong-position matches for `y`.
