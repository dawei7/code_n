## Description

Given two integer arrays `nums1` and `nums2`, return *an array of their <span data-keyword="array-intersection">intersection</span>*. Each element in the result must be **unique** and you may return the result in **any order**.
