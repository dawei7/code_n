## General
Executes a SQL query for **Market Analysis III** using Common Table Expressions (CTEs), relational JOINs, window ranking functions, GROUP BY aggregations.

## Complexity detail
- **Time Complexity**: $O(U + I + O + W log(W))$ — Operation count bound.
- **Space Complexity**: $O(U + I + O)$ — Auxiliary memory allocation bound.
