## Constraints

- The graph contains between `0` and `100` nodes, inclusive.
- `1 <= Node.val <= 100`
- Every node has a unique `Node.val`.
- The graph has no repeated edges and no self-loops.
- The graph is connected, so every node is reachable from the supplied node.
