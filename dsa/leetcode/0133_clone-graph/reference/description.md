## Description

You are given a reference to one node of a connected, undirected graph. Return a deep copy of that graph.

Each graph node stores an integer `val` and a `List[Node]` named `neighbors`. A deep copy must use newly created node objects while preserving every value and neighbor relationship.
