# {Algorithm Name}

| | |
|---|---|
| **ID** | `{challenge_id}` |
| **Category** | {category} |
| **Complexity (required)** | {complexity} |
| **Difficulty** | {d}/10 |
| **Interview relevance** | {r}/10 |
| **Wikipedia** | [{wikipedia title}]({wikipedia_url}) |

## Problem statement

{1-2 paragraphs: state the problem the algorithm solves, the input and
output, and a concrete example with sample input and output.}

## When to use it

- {Use case 1: when this algorithm is the right choice.}
- {Use case 2: a typical real-world setting.}
- {Edge case / warning: when NOT to use it, and what to use instead.}

## Approach

{2-3 paragraphs: the core intuition, the key insight, why it works.
Drawn from the canonical description of the algorithm. Mention the
data structure(s) involved and the invariant maintained at each step.}

## Algorithm (pseudocode)

```
{function name}({params}):
    # 1. {first major step}
    ...

    # 2. {second major step}
    ...

    return {result}
```

## Walk-through

{Step through a 3-7 element input showing the state at each iteration.
Tables and lists work well here. Make it concrete — a player reading
this should be able to re-derive the state.}

## Complexity

| | Time | Space |
|---|---|---|
| **Best** | {…} | {…} |
| **Average** | {…} | {…} |
| **Worst** | {…} | {…} |

{1-2 sentences on the recurrence (if any), the constants, and the
asymptotic class the engine will check against.}

## Variants & optimizations

- **{Variant 1}:** {1-2 sentence description; when to prefer it.}
- **{Variant 2}:** {1-2 sentence description; when to prefer it.}
- **{Optimization 3}:** {1-2 sentence description; tradeoff.}

## Real-world applications

- {Domain 1 (e.g. databases, networking, ML, OS scheduling).}
- {Domain 2.}
- {Domain 3 (e.g. a well-known open-source project that uses this).}

## Related algorithms in cOde(n)

- **[{related_id_1}]({path_1})** — {one-line description} (d={d1}/10, r={r1}/10)
- **[{related_id_2}]({path_2})** — {one-line description} (d={d2}/10, r={r2}/10)
- **[{related_id_3}]({path_3})** — {one-line description} (d={d3}/10, r={r3}/10)

---

*This documentation is original content written for cOde(n),
modeled after the canonical structure used by competitive-programming
reference sites. For the canonical encyclopedia entry, follow the
Wikipedia link at the top of the page. Source repository:
<https://github.com/dawei7/code_n>.*
