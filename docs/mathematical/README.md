# Mathematical Docs Layout

Mathematical references are grouped by source family before category:

- `geeksforgeeks/`: General mathematical foundations for the algorithm categories.
- `neetcode/`: Reserved for future NeetCode-specific mathematical references if those become separate docs.

Keep new docs under `docs/mathematical/{source}/{category}/` so large doc sets remain easy to inspect selectively.
