# Formale mathematische Spezifikation: Wurzel-Blatt-Pfade

Topologische Pfadextraktion mittels Backtracking. Platzkomplexität $O(|V| \cdot \mathcal{H}(T))$. Zeitkomplexität $O(|V| \cdot \mathcal{H}(T))$.